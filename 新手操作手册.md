# DAMN 项目 · 新手操作手册

> **别慌，你只需要照着做 4 件事。** 其他所有信息（跑分、对比、备选方案）都可以先忘掉。
> 遇到任何报错，直接把红字截图发给我，不要自己想办法。

---

## 📍 你现在在哪

```
整体流程（8 步）
──────────────────────────────────────────────
 0. 网络检查 + 配镜像 ......... ✅ 已完成
 1. 跑通第一个画面 ............ 👈 你在这里，今天的目标
 2. 认识项目结构，改一行字 .... 等你完成第 1 步我再讲
 3. 搭 5 个页面骨架 ........... 后面的事
 4. 网络请求层 + 假数据 ....... 后面的事
 5. 接后端真实接口 ............ 后面的事
 6. 本地通知（提醒做任务）..... 后面的事
 7. 打包 APK 保底 ............. 后面的事
──────────────────────────────────────────────
```

**项目已经建好了**，在 `D:\reactnative\damn-app`。
依赖装完了，代码检查也通过了。**现在唯一缺的，就是你把它启动起来看一眼。**

**第 1 步成功的样子**：手机上出现 DAMN 的界面（一个示例页，底部有两个标签）。

---

## 🪜 第一步：打开终端

> **终端是什么？** 就是你给电脑打命令的窗口。和用鼠标点来点去是同一件事的两种做法，不是黑客才用的东西。

**方法 A（推荐，打开就自动在项目文件夹里）**

1. 打开「此电脑」，一层层点进 `D:\reactnative\damn-app`
2. 点一下窗口**最上面那条地址栏**（显示路径的地方）
3. 把里面的字**全部删掉**，输入 `powershell`
4. 按**回车**
5. 弹出一个蓝底或黑底的窗口 → **这就是终端，成功了**

**方法 B（如果 A 不行）**

1. 按键盘 `Win 键 + R`
2. 输入 `powershell`，回车
3. 在黑窗口里输入下面这行，回车：
   ```
   cd D:\reactnative\damn-app
   ```

---

## 🪜 第二步：两条一次性设置（复制粘贴，只做这一次）

在黑窗口里**复制粘贴**下面**两行**，每行按一次回车：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

```powershell
npm config set registry https://registry.npmmirror.com
```

**你会看到什么：**
- 第一条可能会**问你 Y/N**，输入 `Y` 然后回车
- 如果它说「已经是这个值 / 无需更改」，**也没问题，跳过就行**
- 第二条**没有任何提示就是成功**（终端里"没消息"通常等于"成功了"）

> 这两条是干什么的：第一条是允许电脑运行 `npm` 这类脚本（Windows 默认不让，会报"禁止运行脚本"）；第二条是把下载源换成国内镜像，速度快很多。

---

## 🪜 第三步：手机上装 Expo Go

1. 打开手机上的**应用商店**（华为应用市场 / 小米应用商店 / Google Play 都行）
2. 搜索 **Expo Go**
3. 安装它（图标是一个蓝色小方块，发布者应该是 **Expo Project**）

> **Expo Go 是什么？** 它是一个"通用播放器"。你的代码跑在电脑上，手机上的 Expo Go 通过 Wi-Fi 把界面"播"出来。
> 好处：**不需要把 App 安装到手机上**，改一行代码手机上立刻变。

⚠️ **如果商店里搜不到 / 装不上**，**别自己折腾**，直接告诉我，我去找对应的安装包给你。

---

## 🪜 第四步：启动！

在终端里输入：

```powershell
npx expo start
```

**你会看到什么：**
- 一堆字，然后出现一个**大方块二维码**
- 底下还有一些提示，类似 `Press w │ open web`、`Press a │ open Android`

**然后二选一：**

### 方式 1：手机上看（推荐，更真实）
1. 确认**手机和电脑连的是同一个 Wi-Fi**
2. 打开手机上的 **Expo Go**
3. 点 **Scan QR code**（扫描二维码）
4. 对着电脑屏幕上的二维码扫

### 方式 2：浏览器上看（最快）
- 在终端窗口里**按一下键盘 `w`**
- 电脑浏览器会自动打开

---

## ⏳ 重要：第一次会很慢

**第一次打包需要 30～60 秒**，手机上会先白屏一会儿。
**这是正常的，不是在卡死，等它。**

打包过一次之后，后面改代码都是**秒级更新**。

---

## 🔧 如果出问题（只看这三条）

| 现象 | 怎么办 |
|---|---|
| 手机一直白屏 / 显示 Downloading bundle 很久 | **等满 1 分钟**。第一次就是这样 |
| 手机报 `Unable to connect` / 一直转圈 | ① 电脑会弹**防火墙提示**，点**允许访问**（勾上"专用网络"）② 确认手机电脑是**同一个 Wi-Fi**（注意别一个连 5G 一个连 2.4G，名字不同的要连成一样的）③ 还不行就告诉我 |
| 手机红屏 / 报 SDK 版本不兼容 | **截图发我**，这是 Expo Go 与项目版本不匹配，有现成解法 |

**其他任何情况：截图发我。** 不要自己上网搜，网上大部分教程是旧版本的，会把问题搞复杂。

---

## 🎮 跑起来之后可以玩一下（可选）

1. 用 VS Code 打开 `D:\reactnative\damn-app`
2. 打开文件 `src\app\index.tsx`
3. 随便改几个字（比如把某句英文改成 `你好 DAMN`）
4. **保存**（Ctrl+S）
5. 看手机 —— 界面**自动就变了**，不用重启

> 这一步是让你亲身感受"热更新"，也是第 2 步的内容。改了没变化也不要紧，先别纠结。

---

## 📖 名词小词典（看不懂就回来查）

| 词 | 一句话解释 |
|---|---|
| **终端 / PowerShell** | 打命令的窗口 |
| **Expo** | 一套工具，让写手机 App 变简单。我们整个项目都建在它上面 |
| **Expo Go** | 手机上的"播放器"App，用来实时预览你写的界面 |
| **Metro** | Expo 背后的打包工具。`npx expo start` 启动的就是它 |
| **二维码** | 手机 Expo Go 扫它，就能连上你电脑上的项目 |
| **热更新 / 热重载** | 改代码 → 保存 → 界面自动变，不用重启 |
| **SDK** | 一套工具的版本号。我们用的是 Expo SDK 57 |
| **打包 / 构建** | 把代码变成一个能独立安装的 App（`.apk` 文件）。第 7 步才做 |

---

## ✅ 做完第 1 步后，告诉我一句话就行

- 成功了 → 说「跑起来了」
- 失败了 → 把报错截图发我

然后我再带你做第 2 步（认识项目结构）。
