# 交接文档 · 用户基础画像模块（2026-09-22）

> 交接对象：后续继续开发的人 / 评审 / 二轮冲刺。
> 依据文档：`ideas and structures/Prompts/画像.md`、`docs/fin/核心闭环.md`（环节 1/2/3）、`docs/research/RO-2-模型体系.md`（M1/M2/M4 公式）。`docs/design/` 与 `docs/fin/` 同为最终共识，冲突时 `docs/fin/` 优先。

## 一、本次做到了什么

一天内完成用户画像模块**基础部分**，四通道并行开发后统一收口：

| 层 | 内容 |
|---|---|
| 领域（纯规则+EWMA） | `app/domain/profile/`：MBTI 16 型 + `_default` 模板（8 字段，全部【工程】待校准）、硬分类 + 四维百分比加权初始化、`predict_duration`(q50/q80+degraded)、`predict_completion`（冷启动先验 → 0.5×先验+0.5×近7天）、`update_state`(EWMA α=0.3)、`replan_decision`/`should_prompt`/`profile_prompt_json` |
| 持久化 | 新表 `user_profiles`（静态 MBTI）、`user_states`（动态 12 字段 + update_count），仓库 + Alembic 迁移 `cceb72f7ccc0` |
| API | `POST /auth/register`、`PATCH /users/me` 接受 `mbti_type`/`mbti_dims`/`identity`；`GET /users/me/profile`（404=尚未设置画像） |
| 闭环接线（环节3） | 反馈提交 + 任务执行记录 → EWMA 回流更新 state（`feedback_service` / `plan_service._record_execution`） |
| 环节2 画像提示词 | `plan_service._generate` 构建画像 JSON → `PlannerState.profile_prompt` → `plan_generation` 节点拼「画像提示词」段注入 LLM prompt（`app/agent/nodes/plan_generation.py:89`） |
| `user_features` | `plan_service` 懒加载由画像 state 驱动（`ProfileService.build_user_features`，覆盖 duration_factor/energy/stress/slots/sample_size） |
| 前端 | 画像页：4×4 MBTI 选择 + 可选四维滑块 + identity + 保存；状态仪表盘（`degraded` 冷启动徽标）；mock/api 双模式经 `ProfileProvider`，页面零 fetch |
| 文档 | AGENTS.md：`docs/design/` 与 `docs/fin/` 同为最终共识（冲突时 fin 优先） |

## 二、验证状态

- 后端全量 `uv run pytest -q -p no:warnings` → **91 passed**（原 43 + 画像 42 单测 + 6 API）
- `uv run ruff check app tests` → **通过**
- 前端 `tsc --noEmit` / `npm run lint`：新增文件 0 错误（仓库基线有 24 个既有错误，与本模块无关，见 `frontend/src/components/task-row.tsx` 引用缺失 `@/components/task-meta`）
- 迁移 `cceb72f7ccc0` 已在 scratch SQLite 验证可升级

## 三、关键契约（改代码前必读）

- **MBTI 模板**：`app/domain/profile/mbti_templates.py`，16 型 + `_default`，每型 8 键。**数值全部为【工程】拍脑袋值**，后续用数据校准（RO-2 §4.11 升级门槛：S1 需同类 ≥20 条）。
- **加权初始化**：`mbti_dims` 值为 0..1，≥0.5 取该维首字母（I/S/T/J）；数值字段 = `w×解析类型模板 + (1−w)×翻转类型模板`（按维独立混合）；`preferred_time_slots` 取多数类型。
- **PATCH 字段语义（收口时修正过）**：字段**未提交** → 保留原值；字段**显式 null** → 清除；`mbti_dims` 整体替换四列。实现：路由里 `payload.model_fields_set` 计算 `provided` 传给 `ProfileService.upsert_profile`。
- **画像变更重置状态**：`upsert_profile` 用 `init_state` 重新初始化，`update_count=0`（画像.md：初始值全部来自 MBTI 模板）。
- **degraded**：`update_count < 3` 时 `degraded=true`，Scheduler 预留 20% 缓冲（画像.md 可选#2，尚未接入 Scheduler）。
- **前端手写假设**：PATCH 后前端重新 GET；404 当作空态；`degraded` 前端兜底 `update_count<3`。
- **未接入的点（留给后续）**：`degraded`→Scheduler 缓冲；`replan_decision`/`should_prompt` 仅领域实现+测试，未挂 API；MBTI 百分比前端滑块发送已做但后端加权混合对 `ie=0.2` 的少数派混合行为是按契约字面实现（模板锚定已解析类型）；`app/ml/` 统计预测器未动，画像通过 `user_features` 间接驱动。

## 四、文件清单

**新增**：`app/domain/profile/`（4 文件）、`app/infrastructure/database/models/user_profile.py`、`user_state.py`、`repositories/user_profile_repository.py`、`user_state_repository.py`、`app/schemas/profile.py`、`app/application/services/profile_service.py`、`alembic/versions/cceb72f7ccc0_*.py`、`tests/unit/test_profile.py`、`tests/api/test_profile_api.py`、`frontend/src/domain/mbti.ts`、`frontend/src/state/profile-context.ts`、`frontend/src/state/profile.tsx`、`frontend/src/components/profile/`（3 组件）

**修改**：`AGENTS.md`、`app/agent/state.py`、`app/agent/graph.py`、`app/agent/nodes/plan_generation.py`、`app/api/deps.py`、`app/api/v1/auth.py`、`app/api/v1/users.py`、`app/api/v1/mappers.py`、`app/application/services/__init__.py`、`app/application/services/feedback_service.py`、`app/application/services/plan_service.py`、`app/schemas/__init__.py`、`app/infrastructure/database/models/__init__.py`、`app/infrastructure/database/repositories/__init__.py`、`frontend/src/api/types.ts`、`frontend/src/api/endpoints.ts`、`frontend/src/app/_layout.tsx`、`frontend/src/app/profile.tsx`、`frontend/src/data/mock.ts`

## 五、演示路径（答辩）

1. 注册/设置页选 MBTI（如 INTP）→ 保存
2. `GET /users/me/profile` 看初始 state（`update_count=0`，`degraded=true`）
3. 生成目标 → 执行任务 → 提交反馈
4. 再查 profile：`update_count` 增长、`self_efficacy`/`state_energy`/`duration_factor` 被 EWMA 更新
5. 再次生成计划 → LangGraph 拼三段提示词（②画像提示词来自画像 JSON）

## 六、已知事项 / 风险

- `git status`：`docs/fin/` 有 4 个删除文件（api 中文说明等）——**早于本模块**，属此前 docs 重组遗留，未在本次提交中处理，后续自行决定是否提交。
- LSP 对部分 `sqlalchemy`/`fastapi` import 报「could not be resolved」为编辑器未挂 .venv，非代码问题，以 `uv run pytest` 为准。
- `UserModelService` 仍在（统计因子聚合，无调用方），`ProfileService.build_user_features` 以它为基线再覆盖 state 字段。