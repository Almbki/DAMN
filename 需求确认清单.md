# DAMN · React Native 前端框架搭建计划

> 本文件替代原《需求确认清单》。你的答案已填入，待定项保留默认值。
> **执行原则：一步步来，每一步我先讲清楚"这是什么、为什么、你会学到什么"，你确认后我再动手。**

---

## 0. 已确认结论快照（2026-09-18）

| 编号 | 问题 | 你的答案 | 影响 |
|---|---|---|---|
| A1 | 交付时间 | **3 天** | 一切从简，只保演示闭环 |
| A2 | 交付物 | **现场演示** | 见第 2 节（关乎打包，你要求解释） |
| A3 | iOS | **不需要** | ✅ 去掉最大不确定性 |
| A4 | 强制技术要求 | 没有 | 技术自选 |
| A5 | 演示现场网络 | 有网 | 但现场 Wi-Fi 不可信，仍需保底 APK |
| A6 | 会前端/React 的人 | **一个都没有** | ⚠️ **最大风险**，见第 5 节 |
| A7 | 设计稿 | 没有 | 用最朴素的界面 |
| C3 | 消息推送 | **需要** | ✅ 有解法，见第 3.2 节 |
| C 其他 | — | 待定 | 只要有"必须"项，立刻通知我 |
| D1 | 后端框架 | **FastAPI** | 自带 Swagger，能自动生成前端类型 |
| D2 | 接口文档 | 后面补充 | 前端先用 Mock 并行 |
| D3 | 鉴权 | **JWT** | 网络层按 JWT 设计 |
| E2 | 网络/代理 | 待实测（见第 4 节） | 🔴 决定成败 |
| F1 | 系统 | Windows | 只能开发 Android |
| F2 | 仓库 | GitHub | 需要能访问 github.com |
| F3 | 有人接手 | 会 | 代码要写注释 + README |

---

## 1. 我从你脑图读到的产品（请确认我理解对了）

**DAMN** —— 一个由 AI 智能体驱动的「目标拆解 + 精力管理」App：

- 把**不能短时间完成的目标**，自动拆成「时间段内可执行的任务」，并**动态调整任务量**，让人"无感完成"
- 比视频流更智能地**推荐"现在可以做什么"**（借鉴企业项目管理）
- **精力管理**：用几个简单问题判断当前状态，给科学建议（有文献调研支撑）
- 记录用户**心情 / 状态**
- 根据每周期的反馈（**压力感、精力、完成状态**）**动态调整任务量**
- 背后是**可动态改进的智能体**（面临超长上下文、如何从历史中改进、插件机制等问题）
- 技术选型：跨平台 → React；后端 FastAPI + LangGraph

### 1.1 这对前端意味着什么（好消息）

**这是一个"表单 + 列表 + 卡片 + 打卡"的 App，没有任何原生 SDK 依赖。**

- 不涉及相机、蓝牙、地图、支付、IM、音视频
- 所有"智能"都在后端（LangGraph 调 LLM），前端只负责**输入 + 展示**
- 唯一沾原生的是 C3 推送 → 而它本质是「**到点提醒你该做哪个任务**」，属于**本地定时提醒**，不是服务端推送（见 3.2）

**结论：这是 Expo 路线最理想的场景。**

### 1.2 演示需要的页面（我建议的最小闭环，5 页）

1. **状态自评** —— 几个简单问题（精力/心情/压力）
2. **目标输入** —— 用户写下大目标 → 提交给 AI
3. **「现在做什么」** —— AI 推荐的当前任务卡片（产品的杀手锏页面）
4. **任务列表 / 打卡** —— 完成、跳过、反馈
5. **周反馈 → 调整** —— 展示"任务量被动态调整了"

> 5 个页面 = 3 天的极限。**第 3 页（现在做什么）必须做漂亮**，那是评委唯一会记住的东西。

---

## 2. 你问的 A2：为什么"现场演示"关乎签名打包

**核心问题：演示时，App 是跑在你电脑上，还是一个不依赖电脑的独立 App？**

### 情况一：手机上只装「Expo Go」，扫码运行
- **完全不需要签名打包**。JS 代码在你电脑上，手机只是"显示器"。
- 风险：**演示时电脑必须开着、Metro 服务不能断、手机和电脑必须在同一个 Wi-Fi**。
- 比赛现场 Wi-Fi 通常很烂或根本连不上 → **单靠这个是赌博。**

### 情况二：装一个独立 APK（不依赖电脑）—— 推荐做保底

| 包类型 | 需要签名吗 | 说明 |
|---|---|---|
| **debug APK** | 不需要你准备证书 | 用自动生成的 debug keystore 签名，可直接安装。适合内部演示 |
| **release APK** | **需要你自己的 keystore**（.jks 文件 + 密码） | 更小更快，但会暴露一堆只在 release 下出现的问题 |

### "现场演示"这四个字具体影响什么

1. **要不要脱离你的电脑？** → 要 → 必须打包 APK
2. **要不要装到别人的手机上？** → 要 → 必须有 keystore，而且：
   ⚠️ **这个 .jks 文件 + 密码一旦丢失，你永远无法再更新同一个 App**（必须换包名重来）。
   **务必提交进仓库或交给队友保管。**
3. **演示手机之前装过你的 App 吗？** → 装过但签名不同 → **必须先卸载才能装新的**。
   这是现场最容易翻车的点：安装报「应用未安装」，你在台上当场懵。**提前用那台真机试装一次。**
4. **现场网络**：release 包才暴露的坑 —— Android 9+ 默认**禁止明文 HTTP**。
   如果后端是 `http://局域网IP`，release 包里会直接请求失败（debug 包反而可能正常）。
   必须配 `network_security_config` 放行，或后端上 https。

### 我的建议（3 天版）
> **第 1 天：手机直连 Expo Go 扫码跑通。**
> **第 2 天结束前：冻结代码，打一个 release APK 作为保底。**
> **不要留到最后一天打包** —— 打包本身会暴露一堆只在 release 下出现的问题，你需要时间修。
> 现场演示**双保险**：先试扫码，Wi-Fi 不行就立刻装 APK。

---

## 3. 路线决定：**Expo + Expo Go（Android 真机）**

### 3.1 为什么（我先纠正自己上一条说错的话）

> ⚠️ **纠正**：我上一条说"iPhone 装 Expo Go 扫码就能跑，不需要 Mac"—— 这是**过时信息**。
> Expo 官方文档现在写明：**iOS 真机用 Expo Go，需要 Apple Developer Program 付费订阅**，
> 还要自己用 `npx eas-cli@latest go` 构建并通过 TestFlight 安装。
> 依据：[Expo 环境搭建文档](https://docs.expo.dev/get-started/set-up-your-environment/index.md)（我实际抓取核对了）。
> **好在 A3 = 不需要 iOS，这个错误不影响路线**，但必须告诉你我之前说错了。

真正选 Expo 的理由（全部已核对官方文档）：

1. **消除最大风险**：官方文档里「**Android 真机 + Expo Go**」这条路径，**只需要扫码下载 Expo Go**，
   **完全不需要 JDK 17、不需要 Android Studio、不需要 Android SDK**。
   而你机器上恰好一个都没有（装齐要几 GB + 数小时下载）。
   → **今天就能在真机上看到画面。** 这对 3 天倒计时是决定性的。
2. **起步最快**：一条 `npx create-expo-app` 就有可运行项目。
3. **本地通知可用**（见 3.2）。
4. **有退路**：真要原生能力时 `npx expo prebuild` 一键生成 `android/` 原生工程，转 bare 项目，**不被锁死**。
5. 顺带纠正：Expo SDK 57 **不再需要 Watchman**（官方注明"仅 SDK 55 及更早需要"）。

**代价（必须知道）**：Expo Go 只能用 Expo 内置的模块。
一旦需要自定义原生代码 / 真·远程推送，就得做 **development build** —— 那时还是要装 JDK 17 + Android Studio。
**但这个代价被推到了演示之后，是正确的时间点。**

### 3.2 C3「消息推送」怎么办（关键）

官方文档原话：
> **远程推送（push）从 SDK 53 起在 Android 的 Expo Go 中不可用，必须做 development build。
> 本地通知（local notifications）在 Expo Go 中仍然可用。**
> 依据：[Expo Notifications 文档](https://docs.expo.dev/versions/v57.0.0/sdk/notifications.md)

**而你的产品需要的恰恰是本地通知**：

- 「到点了，现在该做 XXX 任务」= `scheduleNotificationAsync` 定时本地提醒 → ✅ **Expo Go 可用**
- 需要服务端主动推的（如"AI 已经拆解完成，去看看"）→ 演示时用**页面内轮询**替代 → ✅ 3 天做得到

**结论：C3 不构成阻塞，也不需要 dev build。** 用 `expo-notifications` 的本地通知即可，甚至很贴产品。

### 3.3 版本

不要盲目上最新。RN 最新 `0.87.1`，Expo 最新 SDK 57（基线 RN 0.86.3）。
我会用 `npx create-expo-app` 的**默认版本**（SDK 57 线），并在动手前先实测安装能否成功。

> 注：reactnative.cn（RN CLI 0.87）要求 SDK Platform 35 + Build-Tools 36，
> 而 Expo 文档要求 Android 16 / SDK Platform 36。两者有出入 —— 但你现在走 Expo Go 路线，
> **暂时都不需要装**，等真要做 dev build 时我再按当时的实测结果定。

---

## 4. 网络实测结果（E2）—— 需要你配合跑一条命令

**我这边测不了，原因必须说清楚**：
我在沙箱里测试时，**TCP 能连通、明文 HTTP 能通、但所有 HTTPS 全部失败**，
`.NET` 报错是 `No credentials are available in the security package`（Schannel 拿不到系统加密凭据），
连 `www.baidu.com` 都失败。**这是我沙箱环境的限制，不是你的网络问题。** 所以这个测试作废。

**已确认的旁证**（这些是真的）：

- DNS 解析正常：`github.com → 20.205.243.166`、`services.gradle.org → 104.16.73.101`
- 你机器上**有代理软件在运行**：`clash-verge`（PID 2680）+ `verge-mihomo`（PID 10188），端口 `127.0.0.1:7897`
- 但 **Windows 系统代理当前是关闭的**（`ProxyEnable = 0`），WinHTTP 也是直连

> ⚠️ 这有隐患：如果 Clash 开着 **TUN 模式**而节点不可用，你的 npm / Gradle 会全部失败。
> 如果 Clash 既没开 TUN 也没开系统代理，等于**没走代理**，访问 GitHub / Google 系就会失败。

### 👉 请你做（复制到自己的 PowerShell 窗口跑，把结果发我）

```powershell
foreach ($u in 'https://registry.npmmirror.com/react-native','https://registry.npmjs.org/react-native','https://github.com','https://dl.google.com/dl/android/maven2/','https://services.gradle.org/distributions/') {
  $r = curl.exe -s -o NUL -w "%{http_code}" --max-time 15 $u
  Write-Output ("{0,-50} {1}" -f $u, $r)
}
```

- 全是 `200` → 网络没问题，直接开工
- `npmmirror` 是 200、其他是 `000` → 正常现象，**全走国内镜像即可**工作
- 全是 `000` → **先修网络，否则什么都做不了**

---

## 5. A6 是最大风险：**团队 0 人会前端**

**3 天 + 零 React 基础 + 现场演示**，这是本项目的真正瓶颈，不是技术选型。

应对（我建议直接执行）：

1. **今天必须让 Hello World 在真机上跑起来**（不改任何代码）—— 第一个里程碑，必须今天达成
2. **技术栈压到最小**：只用 Expo 自带能力，**不引入** Redux / 复杂分层 / TS 严格模式 / ESLint 折腾 / CI / 测试
3. **能塞给后端的都塞给后端**：AI 逻辑全在 Python，前端只做「输入 + 展示」
4. **界面只求清楚，不求好看**：5 个页面，用最朴素的 View/Text + 少量样式
5. **我写代码时同步讲解**，保证你和接手的队友看得懂

**如果 3 天做不完 5 页，砍到 3 页：状态自评 → 目标输入 → 现在做什么。** 这条路径完整体现产品核心。

---

## 6. 分步骤计划（**请你确认后再开始**）

每一步我都会：**先讲这是什么 / 为什么这么做 / 你要学什么 → 你确认 → 我操作 → 你验证**。

| 步骤 | 内容 | 你会学到 | 预计 | 里程碑 |
|---|---|---|---|---|
| **0** | 网络自检 + 配 npm 镜像 + 建 GitHub 仓库 | registry 是什么、镜像为什么必要 | 20 min | 能连上网 |
| **1** | `create-expo-app` 建项目 + 真机扫码跑通 | Expo、Metro、扫码运行机制 | 40 min | 🎯 **手机上看到画面** |
| **2** | 讲解项目结构，你动手改一行字 | 文件路由、组件、样式 | 30 min | 改的字出现在手机上 |
| **3** | 搭骨架：5 个空页面 + 底部导航 | 页面/导航怎么组织 | 1.5 h | 能点着切换 5 个页面 |
| **4** | 网络请求层 + 假数据（Mock） | fetch 封装、JWT 怎么带 | 1.5 h | 页面显示假数据 |
| **5** | 接 FastAPI 真实接口 | 前端怎么和后端对接 | 1.5 h | 显示真实 AI 结果 |
| **6** | 本地通知（"该做任务了"） | 通知权限、定时提醒 | 1 h | 手机弹出提醒 |
| **7** | 打 release APK 保底 + 写 README | keystore、签名、打包 | 2 h | 🎯 **有独立 APK 兜底** |

⚠️ 步骤 1 和步骤 7 是**两个不能省的关键节点**，其他都能砍。

---

## 7. 还需要你补充的（不急，边做边补）

| # | 问题 | 为什么问 |
|---|---|---|
| 新1 🔴 | 演示用的**手机是 Android 吗？Android 几？** | iOS 走不通；Android 版本影响权限与通知行为 |
| 新2 🔴 | 手机能装 **Google Play** 吗？（用来装 Expo Go） | 装不了就得手动下 Expo Go 的 APK |
| 新3 🟡 | 演示时手机和电脑能连**同一个 Wi-Fi** 吗？ | 决定扫码能不能跑通 |
| 新4 🟡 | **D2 接口文档**什么时候能有？ | 决定 Mock 要撑多久 |
| 新5 🟡 | C 类里除了推送，**还有没有"必须"的原生能力**？ | 有的话路线要改，且 3 天不够 |
| 新6 ⚪ | 后端**部署在哪**？（公网 / 局域网 / 本机） | 决定 `http` 明文放行配置 |
| 新7 ⚪ | 演示时**谁来操作**？你还是队友？ | 决定要不要写操作脚本 / 演示流程 |

---

## 附录：本机环境现状（已核对）

| 项 | 现状 | 结论 |
|---|---|---|
| Node | **v24.16.0** | ✅ 满足要求，不用动 |
| npm | 可用（无 yarn/pnpm） | ✅ 用 npm，少装东西 |
| JDK | 只有 `D:\jdk8`，`JAVA_HOME=D:\jdk8` | ⚠️ 走 Expo Go 路线**暂时不需要** JDK 17；真做 dev build 时再装 `D:\jdk17`（**别动全局 JAVA_HOME**） |
| Android SDK / Studio / adb | **全都没有** | ✅ 走 Expo Go 路线**暂时不需要** |
| 代理 | Clash Verge 在跑，但系统代理关闭 | 🔴 见第 4 节 |
| 工作目录 | `D:\reactnative`（含本文件、`Mind map.json`） | — |
| OS | Windows | 只能开发 Android |

**关于 reactnative.cn 那份文档**（你的原始问题）：

- 它讲的是 **RN CLI（bare）路线**，要求 JDK 17 + Android Studio + SDK —— 那是**步骤 7 之后**才需要的东西
- 它默认展示 macOS 分支，Windows 上路径和环境变量设置不同
- 里面的 `npx nrm use taobao` 已过时，直接用 `npm config set registry https://registry.npmmirror.com`
- 它几乎没提 **Gradle 依赖也要换镜像**（国内第一次构建失败的头号原因）
- **照它学没错，但顺序不对**：先 Expo Go 跑起来拿到正反馈，再回头补原生环境知识
