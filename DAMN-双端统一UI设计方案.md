# DAMN 双端统一 UI 设计方案

> 读完你只需要记住 4 句话，细节在下面。**这份文件放在仓库根目录，跟你其他的《需求确认清单》《新手操作手册》在一起。**
>
> **v2 更新（设计风格定案）**：视觉方向定为 **Material 3 骨架 + 轻拟物（柔和外投影 + 12px 圆角）+ 毛玻璃只用于固定浮层**。这一版**推翻了 v1 里"不用阴影、不用卡片"的两条判断**，理由与被保留的纪律都在**第 4.4 节**。

---

## 一分钟版

| # | 问题 | 结论 |
|---|---|---|
| 1 | 手机和 Windows 能不能共用一套 UI？ | **能。** 而且现在这份代码已经具备条件——它没有卡片堆叠、没有阴影、没有图标字体、没有第三方 UI 库，这些正好是跨端最贵的部分。 |
| 2 | 那要改什么？ | **只改外壳**：导航怎么摆、窗口/安全区怎么处理、悬停/键盘/焦点怎么做。5 个页面本身几乎不动。 |
| 3 | 差异放在哪？ | 一个新文件 `src/components/shell.tsx` + 一个「设备能力」Provider。**不允许**把平台差异散进页面里。 |
| 4 | 要不要给桌面加手机没有的功能？ | **不要。** 桌面只能「同时显示更多」和「操作更快」，不能出现「手机做不到的事」。所以命令面板、浮动窗、右键菜单、全屏专注模式全部砍掉。 |

**Windows 端怎么落地**：`react-native-windows` 现在**用不了**。而且「降 RN 版本去凑 RNW」这条路我们要**主动放弃** —— 版本号确实能凑上（RN 0.83 + Expo SDK 55 + RNW 0.83），但 **`expo-modules-core` 在 Windows 上没有实现，并且它自己把 Windows 的 autolinking 关掉了**，这与 RN 是哪个版本无关。完整推演见第 1 节。所以 **Windows 端先用桌面 Web（浏览器打开的宽屏形态）；如果"必须是双击打开的 .exe"是硬需求，走 Tauri 2 套壳，UI 代码零改动。**

**视觉方向**：**Material 的角色制骨架 + 轻拟物（凹/平/凸/浮四档 + 圆角分级）+ 两处毛玻璃**。
配色在 v3 换成**鲜艳琥珀 + 薄荷绿**（旧的棕金 + 灰 sage 显老），毛玻璃调薄到"能看见背后"，并加了**手动明暗切换**。理由见第 5 节。

**桌面端最缺的**：不是布局，是**焦点环**。现在整个 App 一个都没有——键盘用户看不到自己在哪。这是第 1 步要补的。

---

## 1. RNW 可行性：版本能凑上，但生态凑不上

### 1.1 「降 RN 版本」这条路走到了哪一步

我去核了版本号（都是 npm 上的一手数据），结论先给：**降版本确实能找到一对匹配的组合，但它换不来一个能跑的 Windows 应用。**

**版本匹配是成立的：**

| Expo SDK | 它带的 RN | 对应的 RNW | 能不能配 |
|---|---|---|---|
| **57**（本项目） | 0.86.3 | 最新 RNW 0.84.0 要 `react-native@0.84.1` | ❌ 差两个小版本 |
| **56** | 0.85.3（[expo@56.0.22](https://registry.npmjs.org/expo/56.0.22) 的 devDependencies） | 无 0.85 版本存在 | ❌ 太高 |
| **55** | 0.83.10（[expo@55.0.31](https://registry.npmjs.org/expo/55.0.31)） | [RNW 0.83.0](https://registry.npmjs.org/react-native-windows/0.83.0) 的 peer 是 `react-native@^0.83.0` | ✅ **能配上** |

所以「降到 RN 0.83 + Expo SDK 55 + RNW 0.83」在版本号上是一条合法的路 —— **RNW 落后 RN 大约三条版本线，只要退到对应的那条 SDK，版本就能对齐。问题在于对齐之后仍然跑不起来（见 1.2）。**

### 1.2 但真正的 blocker 不是版本，是 Expo 的运行时在 Windows 上没有实现

这一条与版本号无关，无论怎么降都存在：

- **`expo-modules-core` 没有 Windows 实现。** 这是整个 Expo 模块体系的运行时核心。它的仓库目录只有 `android/`、`ios/`、`common/` 三支，**没有 `windows/`**（我查了 `packages/expo-modules-core/` 的完整目录树）。
- 更直接的一条：`expo-modules-core` 自己的 `react-native.config.js` 里，autolinking 被这样写死：

  ```js
  // 原话："Explicitly turn off react-native autolinking for this module"
  module.exports = { dependency: { platforms: { ios: null, android: null } } };
  ```

  也就是说 **Windows 平台根本不会链接它**。而本项目从 `expo-router` 到 `expo-font`、`expo-splash-screen`、`expo-system-ui`、`expo-constants` 全部建立在 `expo-modules-core` 之上 —— **没有它，App 起不来**。
- 配套证据：Expo 官方《Install Expo modules in an existing React Native project》只给 **Android 与 iOS** 两组配置 diff，**没有 Windows 章节**（[docs.expo.dev](https://docs.expo.dev/bare/installing-expo-modules/)）；「把 RNW 认作 out-of-tree platform」在 Expo 仓库里还是**提案 issue**（[expo/expo#47703](https://github.com/expo/expo/issues/47703)）；在 React Native Directory 用 `windows=true` 过滤，`react-native-screens`、`react-native-reanimated`、`expo-symbols` 全都不在结果里（[目录](https://reactnative.directory/?windows=true)）。

### 1.3 「Expo 不兼容」到底会有什么影响（逐项）

假设硬降到 SDK 55 + RN 0.83 + RNW 0.83，你要付的是这些：

| 影响 | 具体是什么 | 严重度 |
|---|---|---|
| **路由要重写** | `expo-router` 的渲染依赖 `react-native-screens`，而它没有 Windows 实现 → Windows 端不能用 expo-router。要么换成 `@react-navigation/native` 手写导航，要么用 `Platform.OS` 分叉出两套路由 —— 后者会开始污染 `typedRoutes`、深链与主题 | 🔴 高 |
| **7 个原生依赖逐个复查** | `react-native-screens`、`react-native-reanimated`、`react-native-safe-area-context`、`expo-symbols`、`expo-glass-effect`、`@expo/ui`、`expo-notifications`。每一个都要确认 Windows 端有没有实现、没有的话怎么降级。`reanimated` 缺席意味着**所有动画要重写**（现在项目里只有 CSS transition，这条反而轻） | 🔴 高 |
| **iOS 直接放弃** | Expo SDK 55 + 旧 RN，与新 SDK 的 iOS 生态开始脱节。你现在不需要 iOS，但这是一个"把路走窄"的决定 | 🟡 中 |
| **安全更新与 bug 修复停止** | RNW 0.83 的维护支持到 **2026-09-30 结束**，Expo SDK 55 也已进入维护期。降级等于**主动把项目钉在一个正在结束支持的版本上** | 🟡 中 |
| **已修的 bug 要重新踩** | 本项目选 57.0.24 的一个原因就是它包含 Hermes 内存/启动回归的修复（见《双端前端架构调研》2.1）。降版本把这些一起退回去 | 🟡 中 |
| **维护成本变成周期性** | Expo 每升一个 SDK，RN 版本就动一次，RNW 又落后三条线 → 每次升级都要重新找那个"三条线都对得上"的版本组合。这是**结构性的**，不是一次性的 | 🔴 高 |
| **本地环境** | RNW 需要 VS 2022 + 四个工作负载 + Windows 10 SDK，数 GB、数小时。你机器上 JDK / Android SDK 都还没装 | 🟡 中 |

### 1.4 结论与替代方案

**结论：不建议为了 RNW 降 RN 版本。** 关键理由不是"麻烦"，而是：**降版本能解决版本号，但解决不了 `expo-modules-core` 在 Windows 上没有运行时这件事** —— 那跟 RN 是 0.83 还是 0.86 无关。

> 如果一定要一个**能双击打开的 Windows 软件**（而不是浏览器里打开的宽屏页面），按代价从低到高：

| 方案 | 代价 | 得到什么 | 我的评价 |
|---|---|---|---|
| **桌面 Web** | 0 | 浏览器里打开的宽屏形态，可存成"应用窗口" | ✅ **推荐**，本文全部设计直接落地 |
| **Tauri 2 套壳** | 小 | 真正的 .exe，用系统 Edge WebView2 渲染，体积几 MB 级 | ✅ 若"必须是 .exe"是硬需求，选这个。加载的正是同一份 Web 构建，**UI 代码零改动** |
| Electron 套壳 | 中 | 同上，但内置 Chromium | ⚠️ 可行，体积上百 MB，UI 代码同样零改动 |
| **RNW（降版本）** | **大** | 原生 Windows 手感 | ❌ 见 1.1–1.3 |

Tauri 有一条前提要提前核实：**目标机器上有没有 WebView2 运行时**（Win11 基本自带，Win10 视版本可能需要装一次；Tauri 也允许把运行时打包进去，代价是体积）。这件事我**没有实测**，真要走这条路之前要验。

**所以本文档的落点不变**：Windows 端先做**桌面 Web**。而且下面这套设计本身就是"将来换任何外壳"的前提条件 —— 一个不依赖 Web 专属 API 的外壳层，换 Tauri 或换 RNW 时改的都只是外壳的那两个文件。

> 一句话：**不为 RNW 降版本，但按「外壳可替换」的纪律写代码。**

---

## 2. 双端差异的判据

桌面端**不需要**手机做不了的能力，而且不应该有。据我理解，你这句话其实是在质疑一个默认假设：一提到"桌面端"，设计文档就自动开始加侧栏、加多窗口、加命令面板——那套默认动作的代价是产品裂成两个。

**判据（三条，按优先级）：**

1. **显示更多** —— 宽屏可以同时显示更多，但**每一样在窄屏都能拿到，只是要多一步**（点开 / 滚动）。
   例：桌面右栏常驻显示「为什么是这件事」；手机上它在这张卡片下方，点一下展开。同一份文案、同一个组件。
2. **操作更快** —— 桌面可以给动作配键盘、悬停；但**每个动作必须先能在触屏上完成**，键盘只是加速器，不是唯一入口。
3. **不许有独占能力** —— 只要出现"桌面上能做、手机上做不到"的功能，它就不是适配，是新功能。

**用这条判据砍掉的：**

| 被砍的 | 为什么砍 |
|---|---|
| 命令面板（⌘K） | 导航只有 5 项、动作只有 3 个。为 8 个目标做模糊搜索面板，是给"桌面感"交的智商税。 |
| 常驻迷你浮动窗 | 手机无对应物，违反判据 3。 |
| 右键菜单 | 每行的动作只有「今天先不做」一个，悬停直接显出来**更快**，还少一套菜单实现。 |
| 全屏专注模式 | 演示价值高，但它把「现在做什么」变成了第二个页面。留档，不实现。 |

**保留的桌面增强（都满足判据 1 或 2）**：右侧「为什么 / 接下来」栏、`Enter` 开始、`Esc` 返回、`空格` 勾选、`J/K` 上下移动、焦点环、悬停显出的「今天先不做」。全部是**加速**，全部有触屏等价路径。

---

## 3. 现状盘点：哪些资产已经是对的（不用重做）

逐文件看过：

| 现状 | 判定 | 依据 |
|---|---|---|
| `constants/theme.ts` 的 primitive → semantic 分层 | ✅ 保留 | 明暗两套 `ColorScheme` 已经齐了；`ThemeColor` 类型保证组件不写颜色字面量 |
| 「冷灰白底 + 发丝线 + 金只给现在 + 鼠尾草只给完成」 | ✅ 保留 | 这是差异化本身，双端通用，与平台无关 |
| 窄屏底栏 / 宽屏侧栏的两档响应式（`Breakpoints.sidebar = 768`） | ✅ 方向对，**档位不够** | 见第 5 节，要拆成 4 档 |
| `useIsWide()` 首帧强制窄屏（防 hydration 崩溃） | ✅ 保留 | `hooks/use-theme.ts` 里已写明原因，这是修过 bug 的 |
| 5 个页面全靠 `View`/`Text`/`Pressable`，无卡片、无阴影、无图标库 | ✅ 保留（**但视觉语言按第 5 节升级**） | 这是跨端最便宜的一层，换壳时不用动。阴影/圆角/玻璃由新增的 `Surface` 统一提供，页面本身仍然只用基础组件 |
| 字重只用 400 / 700 | ✅ 保留 | Android 中文字体只有这两档 |
| `src/global.css` 的 CSS 变量喂给 `Fonts.web` | ⚠️ 仅 Web | 原生端走 `Platform.select` 分支，这是允许的 |
| `_layout.tsx` 里 `document.createElement('link')` 加载字体 | ⚠️ 仅 Web | 已用 `Platform.OS === 'web'` 护住，但它是"页面级 DOM 操作"的先例，**不能再扩散** |
| `app.json` 的 `orientation: "portrait"` | ❌ 要改 | 它会把桌面窗口锁成窄条 |
| `Page` 的 `maxWidth` 只有 640 / 760 | ❌ 要改 | 宽屏无上限地留白，且没有承载辅栏的位置 |
| 全项目没有 focus ring | ❌ 要补 | 桌面键盘用户看不见自己在哪 |
| `@expo/ui`、`expo-glass-effect` 已装但未引用 | ⚠️ 保持不引用 | 平台字段只有 android/ios 与 ios/tvos，**都没有 Web**，将来也不会有 Windows |

**新增三条编码红线**（为了让"同一套 UI 将来还能换壳"成立）：

1. **页面里不出现 DOM**：不 import `react-native-web` 专属 API、不写 `global.css` 类名、不碰 `document` / `window`。字体、CSS 变量这类只能在 `constants/` 的平台分支里出现。
2. **不用 Web-only 或各端不一致的样式属性**：`cursor`、`outline`、`backdrop-filter` 都只有 Web 支持 → 一律封成跨端组件（`FocusRing`、`Hoverable`、`Surface variant="glass"`）。**注意 `boxShadow` 不属于这一类**：RN 0.86 对 iOS 与 Android 都支持它（含 `inset`），所以它是我们跨端统一使用的阴影写法（见 5.2）；真正只有 iOS 生效的是老的 `shadowOffset / shadowOpacity / shadowRadius`。
3. **`Platform.OS` 只允许出现在 `components/shell*` 与 `constants/` 里**。页面里出现就算违规——宽度差异用 `useLayout()`，能力差异用 `useCapabilities()`。

---

## 4. 设计 token 扩展

现有 token 不动（`Palette` / `Colors` / `TypeScale` 全部保留）。新增的放 `src/constants/tokens.ts`，`theme.ts` 保持"颜色 + 排版"，避免一个文件继续膨胀。

### 4.1 颜色：只加不换

| 新 token | 亮色 | 暗色 | 唯一的用途 | 为什么必须加 |
|---|---|---|---|---|
| `accentLine` | `#8A5A0B` | `#D8A954` | **焦点环、当前项的强调线**（= 现在的金，单列出来给一个明确语义） | 现在 `accent` 同时兼任"现在这件事"和"选中态"，宽屏上同屏出现会互相削弱 |
| `warning` / `warningWash` | `#8A5A0B` / `#F5EDDD` | `#D8A954` / `#2E2A20` | 「这周排不下」这类**非惩罚性**提示 | 产品立场是"调低任务量是功能"，不能用 `danger` |
| `scrim` | `rgba(22,24,28,0.32)` | `rgba(0,0,0,0.56)` | 手机端辅栏展开时的遮罩 | 现在没有遮罩层 |
| `interactionHover` | `rgba(22,24,28,0.04)` | `rgba(236,238,240,0.06)` | 悬停叠层 | 桌面必须有悬停反馈，且**不能**靠换填充色（会破坏"发丝线"的分区逻辑） |
| `interactionPressed` | `rgba(22,24,28,0.08)` | `rgba(236,238,240,0.10)` | 按压叠层 | 现在靠 `opacity: 0.6`，宽屏大色块上会显得整块发灰 |
| `surface1` / `surface2` / `surface3` | `#FFFFFF` / `#FFFFFF`＋阴影 / 见 5.2 | 同左 | **抬升面的三档容器色**（Material 的 `surface-container` 思路） | 初版只有"页面底"和"抬起面"两级，做不了轻拟物需要的层次。暗色下靠**面上叠白**（`rgba(236,238,240,0.03/0.05)`）而不是加深，因为暗色下阴影几乎不可见 |
| `colorKey`（预留） | 待定 | 待定 | 给「精力评估」这类非主色强调场景 | 见 5.4 —— 这一格我故意**留白**，不为了配色而配色 |

> **对比度自查（我算过）**：`textFaint = #9AA0A9` 在页面底 `#F2F4F6` 上只有 **2.39:1**，在抬升面 `#FAFBFC` 上 2.54:1，都远低于 AA 正文要求的 4.5:1。而它现在用在**用户要读的内容**上：时间戳（「精力中等」）、已完成任务的删除线标题。
>
> 想靠加深解决，得一直加深到 `#6A6F78`（4.58:1）才够——而那正好是现在 `textMuted` 的值。也就是说**浅色主题里"第四级文字"这个层次本身是不存在的**。
>
> 所以只有两个选项：**A.** 让 `textFaint` 在浅色下等于 `textMuted`（承认没有第四级文字）；**B.** 保留它，但**限制在非文字用途**（分隔线、刻度尺、未选中图标），文字一律用 `textMuted`。我建议 **B**。
>
> 顺带：暗色下 `nightFaint = #6E747C` 在 `#15171A` 上是 3.81:1，同样不够；`nightMuted = #9BA1A9` 是 6.9:1，够。**这是两种配色共有的问题。**

### 4.2 尺度：加宽屏档，不加字号档

**字号一套到底**（`TypeScale` 不加桌面档）。理由：RN 里的字号是**物理尺寸**，而桌面视距并不比手机远（笔记本 50–70cm vs 手机 30–40cm），所以"桌面字号小一号"是伪密度；真正的密度来自**更紧的行高 + 更宽的一屏**。这也顺便避免了"两套字阶"这个双端漂移的最大来源。

| 新 token | 值 | 用途 |
|---|---|---|
| `Spacing.nine` | `96` | 宽屏页面顶/底留白 |
| `Layout.railWidth` | `60` | 宽屏左侧导航栏宽度（只有强调线 + 2–3 字标签，比现在的 208 侧栏窄得多） |
| `Layout.detailWidth` | `280` | 右侧辅栏宽度 |
| `Layout.topBarHeight` | `44` | 手机顶部标题行（也是 `_layout.tsx` 现在缺的那层安全区） |
| `Layout.pagePaddingWide` | `32` | 宽屏页面内边距（窄档继续用现有的 `Spacing.five` = 20） |
| `Layout.mainMaxWidth` | `760` | 主列上限（= 现状的 `MaxContentWidthWide`，**排版底线，双端一致**） |
| `Layout.gridMaxWidth` | `1200` | 主列 + 辅栏 + 间隙的总上限 |
| `Layout.minWindowWidth` | `360` | 低于此宽度不保证布局；`app.json` 的竖屏锁要去掉 |
| `LineHeight.dense` | `字号 × 1.5` | 宽屏正文行高（现在统一 1.6 倍，宽屏收到 1.5；中文仍 ≥1.5） |

### 4.3 交互态：把"能力"变成 token

```ts
/** 设备的输入能力。页面用它决定"显示什么"，不用它决定"长什么样"。 */
export type Capabilities = {
  hover: boolean;            // 有精确指针（鼠标）→ 可以悬停
  keyboard: boolean;         // 有物理键盘 → 启用快捷键、显示快捷键提示
  resizableWindow: boolean;  // 窗口可自由改变尺寸 → 允许出现侧栏
  touch: boolean;            // 有触屏 → 保证 44pt 命中区
};

export const FocusRing = {
  width: 2,
  offset: 2,
  radius: Radius.sm,   // 焦点环必须跟随元素圆角，不要另设一套
} as const;
```

`touch` 与 `hover` **不是互斥的**：触屏笔记本两个都为真。所以判据永远用 `hover`，不要用 `Platform.OS === 'web'` 推。

---

## 5. 视觉语言：Material 的角色制 + 轻拟物 + 两处毛玻璃

**三条边界条件先摆在前面**（都是实测或官方文档，不是审美偏好）：

| 边界 | 事实 | 后果 |
|---|---|---|
| 暗色里没有阴影可投 | 页面底 `#15171A`，抬升面 `#1D2024` 相对底只有 **1.10:1** 的亮度差 | 暗色必须换手法：**表面抬升 + 顶部 1px 高光内描边 + 发丝线**表达"受光"。这是拟物语义的等价表达，**不是放弃拟物** |
| "凸起"是交互信号，不是装饰 | 我把阴影色按背景派生了 7 档来量：`#E3E7EB` 与页面底的亮度差 0.108（柔投影区间），`#CCD2D9` 到 0.263（已经像浮起的卡片） | 凸起**只能给真正可点的东西**。守住这条，拟物反而比 M3 的 elevation 更有用（M3 的 elevation 只表示层级，不表示可操作性）；守不住，用户会去点一切凸起的东西——这是本方案最大的减分项 |
| Android 的模糊要额外接线 | `expo-blur` 官方（SDK 57）明确：Android 上必须用 `BlurTargetView` 包住被模糊的内容、把 ref 传给 `BlurView`；且只有 **Android 12（SDK 31）以上走 RenderNode 才高效**，更老版本"uses the much less efficient RenderScript API" | 毛玻璃只能用在**固定位置的浮层**，且老设备要显式降级（见 5.5） |

这一节是对你那条方向意见的正面回应，也**改掉了初版的一条硬规则**。初版写的是「不用卡片堆叠、不用阴影、结构只靠发丝线」；现在改成「**静的是线，动的是面**」。我先说为什么我认为该改，再说改了之后哪些东西必须守住。

### 5.1 为什么我收回初版那条规则

初版那条规则的理由是「防生成式设计的默认长相」——圆角卡片 + 统一阴影 + 渐变确实是最容易被认出来的模板脸。但那个理由**只对"把内容切成一堆同样的卡"成立**，对"层次"不成立：

1. **桌面端需要层次。** 手机上只有一层（内容），发丝线够用。桌面长出了导航栏、主列、辅栏、悬浮控件四层，纯发丝线在视觉上会把它们拉平——用户分不清"这块是钉住的"还是"跟着滚的"。
2. **阴影本来就是"轻"的语言。** 你的产品立场是"松一口气"，不是"加油"。圆角 + 一档柔和阴影传达的正是"柔和、不逼人"，这跟产品立场是**同向**的，比硬边发丝线更贴。
3. **状态层比颜色更适合这个产品。** Material 的 hover/pressed 用半透明叠层而不是换填充色——这与"金色只给现在"的纪律天然兼容：**悬停不需要动用一个新颜色**。

所以我不是被"Material 好看"说服的，是被"**我们要在宽屏上表达层次，而层次不能靠换颜色**"说服的。

### 5.2 四档高度：真阴影，但只有四档

**轻拟物的关键不是"凸起"，是"凹与凸成对出现"。** 只做凸起（外投影）= 悬浮卡片；只有再加一档**凹（inset 内阴影）**，才是"轻拟物"，也才补上 M3 缺的那一格：**凸 = 可点，凹 = 已经发生的事**（按下了、选中了、已在编辑）。

阴影按**功能**分档，不按"好看程度"分。这是整套视觉里最需要纪律的地方：

| 档 | 语义 | 用在哪 | 亮色 | 暗色 |
|---|---|---|---|---|
| **0 · 平面 flat** | 可读，不可点 | 页面底、列表行、区块、状态条、分隔线 | 无阴影，**发丝线仍然是分区的主角** | 同左 |
| **1 · 抬起 raised** | **可点** | 主按钮、当前这张任务卡、辅栏容器、列表容器、输入聚焦时的面板 | `0 1px 2px rgba(22,24,28,0.06), 0 4px 12px rgba(22,24,28,0.06)` | 阴影不可见 → **面上叠白** `rgba(236,238,240,0.03)` + 发丝线 |
| **2 · 浮层 overlay** | 浮在内容之上 | 弹层、底部抽屉、浮动操作条 | `0 8px 24px rgba(22,24,28,0.12)` | 同 1 的替代策略，叠白提到 `0.05` |
| **3 · 凹 inset** | **已发生** | 按下态、状态自评的选中档位、输入框聚焦时的槽、已勾选的行 | `inset 0 1px 2px rgba(22,24,28,0.10), inset 0 -1px 1px rgba(255,255,255,0.85)` | `inset 0 1px 2px rgba(0,0,0,0.45), inset 0 1px 0 rgba(255,255,255,0.04)` |

三条派生规则（写进 `tokens.ts` 注释）：

1. **阴影色由背景派生，不用纯黑，也不用暖棕。** 页面底是冷灰白 `#F2F4F6`，所以主阴影用冷灰 `rgba(22,24,28,0.06)`。**刻意避开暖棕阴影**（`rgba(120,80,40,·)` 那类）—— 暖调阴影是"AI 生成感"的头号配色特征，而本项目的底是刻意选的冷灰。
2. **暗色不做 drop shadow，改用"顶部高光内描边 + 叠白"**：`borderTopWidth: StyleSheet.hairlineWidth` + `rgba(236,238,240,0.06)`。这是把拟物的"受光"翻译到暗色，比硬投一层看不见的阴影诚实。
3. **同一个元素上不许同时出现「阴影」和「毛玻璃」**（见 5.5 纪律 1）；**焦点环永远盖过阴影**（焦点是可达性问题，优先级高于任何质感）。

纪律三条：

1. **静态内容不配阴影。** 列表行、自评的三个问题组、回顾页的条形图 —— 这些用发丝线。**"把每一行都做成圆角卡片加阴影"是本方案明确禁止的**，那是模板脸，也会让手机端一屏从 6–8 条掉到 3 条。
2. **阴影只有这四档**，组件里不许出现自定义阴影值。跨端实现细节（**这条我改过一次，请以这里为准**）：

   > `boxShadow` **不是 Web-only**。RN 0.86 官方《Shadow Props》把阴影分成三套 API：`boxShadow`（**iOS 与 Android 都支持**，且支持 `inset` 关键字与 `spreadDistance`）、`filter: dropShadow`（仅 Android）、以及老的 `shadowColor / shadowOffset / shadowOpacity / shadowRadius`（**除 `shadowColor` 外都只有 iOS 生效**）。
   >
   > 所以我们的选择是明确的：**统一用 `boxShadow`，因为它对三种目标（iOS / Android / Web）都能表达"柔和外阴影 + 内阴影"**，而 `inset` 正是"凹"这一档唯一能跨端的写法。真实是 Web-only 的是 `cursor`、`outline`、`backdrop-filter` —— 这三个必须封在 `Surface` / `FocusRing` 里。
   >
   > 这层差异**封在 `Surface` 组件里，不外泄**。低端 Android 上 `boxShadow` 靠渲染合成，长列表**不要**逐行加阴影（这也是纪律 1 的另一半原因）。
3. **暗色不用阴影表达抬升**：用叠白的面 + 发丝线。原因是暗色下阴影几乎不可见（现状注释里已经写过这条）。

### 5.3 圆角分级：让它承载层级

初版圆角只有 4/8/12/full，理由是"大圆角是生成式设计的特征"。现在在 `tokens.ts` 里放开两档，**但它们必须跟着层级走**——同一个圆角用在所有东西上，才是模板脸：

| 值 | 用在哪 |
|---|---|
| `4` | 行内元素：小标记、刻度、条形图端点 |
| `8` | 控件：按钮、输入框、勾选框容器、**凹（inset）元素** |
| `12` | 列表容器、区块面 || **`16`（新增）** | 卡片、辅栏容器、抽屉 |
| **`20`（新增）** | 弹层、底部抽屉、浮动条 |
| `full` | 圆形：完成键、刻度点 |

一句话：**越大越浮**，而不是"看起来更现代"。注意 `16 / 20` 加在 `tokens.ts` 的 `Radius` 里（`theme.ts` 的 4/8/12/full 不动），这样第 4 节那行"圆角只有 4/8/12 与 full"的旧约束不会自相矛盾 —— **以本表为准**。

**凹（inset）的圆角要比它所在的容器小一档**：凹面是"陷进去"的，如果圆角跟外层一样大，视觉上会像两个独立的面而不是一个坑。

### 5.4 Material 里我抄什么、不抄什么

**抄（骨架层）：**

| 抄的 | 为什么 |
|---|---|
| **角色制色**：组件引用的是 `surface / on-surface / primary / outline` 这类**角色**，不是色值 | 这正是现有 `ColorScheme` + `ThemeColor` 的做法，等于把已经做对的事做成显式规矩 |
| **状态层**（hover/pressed/disabled 用半透明叠层） | 与"金色只给现在"兼容，悬停不消耗新颜色 |
| **5 档 surface 分层**（页面底 / 抬起面 / 卡片面 / 浮层面 / 遮罩） | 轻拟物需要的层次就靠这五档，不靠五种颜色 |
| **可达性下限**（触控目标、对比度、focus ring） | Material 的硬性部分本来就是行业底账 |

**不抄（否则就变成"又一个 Material App"）：**

| 不抄的 | 原因 |
|---|---|
| Material 3 的默认配色与 **dynamic color** | 品牌色（金 / 鼠尾草）是差异化，不能被系统取色替换掉 |
| **彩色 tonal 填充块**（`primary-container` 那种淡紫/淡青色块铺一片） | 颜色一多，"金=现在、绿=完成"的语义就废了。语义色只允许出现在**需要语义**的地方 |
| 大圆角 + 全卡片化 | 见 5.2 纪律 1 |
| 从底部堆叠的 FAB | 本产品的主动作是**一个明确的「开始」**，不是"随时创建"；FAB 表达的是后者 |
| 密集的横滑 Tab / 面包屑 | 5 个页面用导航栏就够，桌面也不需要第二套导航元语言 |

**结论**：Material 负责**层次与状态**，本产品负责**色彩与语气**。这两者不冲突，因为 Material 最强的是前一半。

### 5.5 毛玻璃：只用两处，而且不做大范围背景模糊

你想要的"通透感"我同意，但**必须限流**，理由是三条实测边界：

- `expo-glass-effect` 的平台字段是 `ios/tvos` —— **Android 与 Web 都不支持**（本项目正好没有 iOS）；
- `expo-blur` **双端都能用**（SDK 57 的平台字段是 Android / iOS / tvOS / **Web**），但 Android 上有两个附加条件：① 必须用 `BlurTargetView` 包住被模糊的内容、把它的 ref 传给 `BlurView`（否则得到的是一个半透明视图，不是模糊）；② **只有 Android 12（SDK 31）以上走 RenderNode 才高效**，更老版本"uses the much less efficient RenderScript API"，官方建议用 `blurMethod="dimezisBlurViewSdk31Plus"`（老系统直接退化为不模糊）。另有一条官方已知问题：`BlurView` 渲染在动态内容（如 `FlatList`）**之前**时模糊不会更新 —— 所以它只能贴在**固定位置的浮层**上；
- 半透明层叠在滚动的文字上，对比度会**随滚动内容变化**，这是可达性上的真问题（不是审美问题）。

所以毛玻璃定为**两处**，且**都不承载正文**：

| 用在哪 | 形态 | 各端降级 |
|---|---|---|
| **桌面左导航栏**（内容从它下面滚过） | 浅色 `rgba(255,255,255,0.72)`／**深色 `rgba(42,46,51,0.70)`** + 背景模糊 + 一条发丝线 | Web 用 `backdrop-filter`；**Android 用 `BlurTargetView` + `dimezisBlurViewSdk31Plus`**；SDK 31 以下退化为不透明面 |
| **手机底部导航栏** | 同上，上缘发丝线 | 同上 |

### 5.5.1 深色必须换一种玻璃（实测，别凭感觉）

你担心的"深色效果会不会不好"——**会，而且比想象的严重**。原因是**玻璃和文字在抢同一个亮度方向**：深色下文字是浅的，玻璃越白，字越糊。把浅色那套白玻璃直接搬过去，等于做出一块"浅色面板上放浅色字"：

| 深色方案 | 玻璃面实测 | 玻璃上的文字对比 | 玻璃 vs 页面底 |
|---|---|---|---|
| 白 `0.72`（浅色那套直接搬） | `#BDBEBF` | **1.60:1** ❌ 读不了 | 9.65:1（很抢眼） |
| 白 `0.50` | `#8A8B8C` | 2.93:1 ❌ | 5.26:1 |
| 白 `0.30` | `#5B5D5F` | 5.69:1 ⚠️ | 2.72:1 |
| 白 `0.10` | `#2C2E31` | 11.71:1 ✅ | 1.32:1（几乎看不出） |
| **抬升白 `#2A2E33` @ `0.70`**（本方案） | `#24272C` | **12.88:1** ✅ | **1.20:1**（刚好看出有一层） |
| 深墨 `#0E1013` @ `0.72` | `#101215` | 16.13:1 ✅ | 1.04:1（看不出来） |

结论：**深色叠"抬升白"，不叠纯白。** `12.88:1` 说明字稳住了，`1.20:1` 说明那层玻璃刚好能被看出来——再淡就看不出，再浓就吃文字对比度。这也说明深色下"白玻璃"为什么走不通：它的两个目标（玻璃显眼 / 文字可读）本身互相冲突，而"叠抬升白"把冲突压到了可接受区间。

浅色侧复算：白 `0.72` 叠在页面底 `#F2F4F6` 上得 `#FBFCFC`，主文字 `#16181C` 在其上 **17.29:1**，玻璃本身不吞对比度；浅色真正的风险是滚动到底下的暖金内容（`#8A5A0B` / `#D8A954`）经过导航栏时，所以 **`0.72` 是浅色 alpha 的下限**。

**不做**：整页背景模糊、卡片毛玻璃、弹层后面的模糊、滚动的玻璃装饰。

配套纪律：

1. **毛玻璃和阴影不能同时挂在同一个元素上** —— 两者都在表达"浮起来"，一起用是重复信号，还会让低端机同时吃两次合成开销。
2. **玻璃面上的文字必须用不透明前景色**，且对比度按**最坏情况**（滚动到最深色内容时）验算，不按平均情况。
3. `backdrop-filter` 是 Web-only，所以它必须封在 `Surface variant="glass"` 里：Android 走 `BlurTargetView` 分支、老系统走不透明分支、将来 RNW 走第三条 —— 这条正好是第 3 节"三条编码红线"的直接应用。
4. **玻璃不许用在会滚动的元素上**（官方那条"渲染在 `FlatList` 之前模糊不更新"的坑），只允许 `position: sticky/fixed` 的浮层。

### 5.6 这条方向对产品气质的影响（一句提醒）

Material + 阴影 + 玻璃的默认观感是"现代、通用、有点科技感"，**而这正好是 DAMN 最不需要的气质** —— 它卖的是"松一口气"，不是"效率工具"。所以差异化必须**集中守住两处**：

1. **那个抓眼的字阶**：时长数字仍然是全页最大元素（拉丁衬线 + 等宽数字），这是唯一不能被 Material 化的地方。
2. **金色与鼠尾草的语义独占**：金色只给「现在」，鼠尾草只给「完成」。Material 的角色制很容易让人顺手多引入几个颜色，**一多就废**。

其余部分（面、阴影、圆角、状态层、表面色）尽量贴 Material 的标准做法 —— 那部分越标准，你越省事，也越不容易出错。

---

## 6. 布局：4 档宽度，宽屏只加"辅栏"

### 6.1 分档

| 档 | 宽度 | 导航 | 内容结构 | 主列宽 |
|---|---|---|---|---|
| **C** compact | `< 700` | 底部标签栏（现状） | 单列 | = 视口 − 40 |
| **M** medium | `700 – 1099` | **左侧栏，宽 60**（竖排短标签） | 单列 + 右下方「详情」区 | ≤ 640 |
| **E** expanded | `1100 – 1439` | 左侧栏 60 | **主列 760 + 间隙 40 + 辅栏 280** | 760 |
| **W** wide | `≥ 1440` | 左侧栏 60 | 同上，整体（1000）**居中** | 760 |

```ts
// constants/tokens.ts —— 唯一的档位判定函数，页面不许自己写 width 比较
export type LayoutMode = 'compact' | 'medium' | 'expanded' | 'wide';
export function layoutMode(width: number): LayoutMode { /* ... */ }

// ⚠️ 静态渲染阶段没有真实窗口宽度（Dimensions 默认 width: 0），
// 所以首帧必须落在 'compact'。这与现有 useIsWide() 一致 ——
// 那里已经因为"首帧用真实宽度"引发过一次 hydration 崩溃，不要重犯。
```

为什么把侧栏下限从 768 提到 700、宽度从 208 收到 60：**700–1100 这一段的窗口（平板竖屏、笔记本半屏）不配拥有两栏**，但也不该留着 208px 的侧栏去挤内容列。

为什么 E/W 两档要居中：内容总宽 1000 在 1440 上只占 70%，靠左会让右侧空出一大块没有任何结构的灰面。居中后左右留白对称；**主列本身仍然左对齐**，读者视线起点不变。

```
C  (<700)               M  (700–1099)          E/W  (≥1100)
┌──────────────┐        ┌───┬────────────┐      ┌───┬───────────────────────┐
│  14:30  精力 │        │   │  14:30     │      │   │   14:30   精力中等    │← 主列顶行
│              │        │ 现 │            │      │ 现 ├───────────────┬───────┤
│  25          │        │ 在 │   25       │      │ 在 │   25 分钟     │ 为什么 │
│  分钟        │        │ 做 │   分钟     │      │ 做 │   任务名      │ 是这件 │
│  任务名      │        │ 什 │   任务名   │      │ 什 │               │ 事？   │
│              │        │ 么 │            │      │ 么 │               │       │
│ ▌为什么是…   │        │   │ ▌为什么…   │      │   ├───────────────┤ 接下来 │
│              │        │ 今 │            │      │ 今 │ [ 开始 ]      │ 15:00 │
│ [   开始   ] │        │ 天 │ [ 开始 ]   │      │ 天 │ 今天先不做这… │ 15:40 │
│ 今天先不做…  │        │ 的 │            │      │ 的 ├───────────────┴───────┤
│              │        │ 任 │            │      │ 任 │ 到 15:00 只有 25 分钟 │
│              │        │ 务 │            │      │ 务 │ 精力中等，够用       │
│ ──────────── │        │   │            │      │   │                      │
│ 现在 状态 任务│        │ 上 │            │      │   │                      │
│ 目标 回顾     │        │ 周 │            │      │   │                      │
└──────────────┘        └───┴────────────┘      └───┴───────────────────────┘
   底栏 5 项              竖栏 5 项（60px）        主列 760 + 辅栏 280
```

### 6.2 网格：一个组件说了算

```tsx
// components/shell.tsx —— 唯一知道"档位"的地方
<AppShell>
  <NavRail />                      {/* C 档渲染到底部，其余渲染到左侧 */}
  <ContentGrid detail={detail}>    {/* 传入辅栏内容；C/M 档它落到主列下方 */}
    {children}                     {/* 主列，maxWidth 760，永远左对齐 */}
  </ContentGrid>
</AppShell>
```

规则：

- `ContentGrid` 在 C/M 档把 `detail` 渲染成粘在主列下方的区块（**不消失、不作障**）；E/W 档才挪到右侧成为独立列。
- 辅栏在 Web/桌面上是 `position: sticky`（滚动时钉住）；原生端不支持 sticky，退化为跟随滚动。这是允许的差异——它只影响"滚动时还在不在"，不影响"能不能看到"。
- **不允许**任何页面自己判断宽度后决定"要不要显示某个信息"。能藏的只能是**排版细节**（如刻度尺在窄屏收成一行数字），不能是**信息本身**。

---

## 7. 逐页设计

每页三种形态：**手机（C 档）** / **平板与窄窗口（M 档）** / **桌面（E、W 两档同形）**。

### 7.1 `index` —— 现在做什么（灵魂页）

**设计立场不变**：时长是全页最大元素，因为用户卡住的不是"做什么"，而是"要花多久"。

| 档 | 形态 |
|---|---|
| C | 顶行「14:30 · 精力中等」→ 大数字 25 分钟 → 任务名 → 刻度尺（收成 `第 3/12 步` 一行）→ 发丝线 → 「为什么是这件事」→ `[开始]` + 「今天先不做这件」。全部在一屏内。 |
| M | 同 C，只是导航变竖栏（内容列仍 ≤640）。刻度尺恢复成竖排 12 根。 |
| E/W | 主列同上；**右辅栏**两块：① 为什么是这件事（同一段文案 + 归因门槛说明）②「接下来」= 后两件任务的时间与标题。刻度尺保持竖排。 |

「接下来」为什么不放进主列：它会跟主列的 CTA 抢注意力。放在辅栏里，它是**余光可见、需要时才看**的第二信息——这正好是宽屏该干的事（显示更多，但不加权重）。

### 7.2 `tasks` —— 今天的任务

| 档 | 形态 |
|---|---|
| C | 一屏 6–8 条。每行：圆形完成键 + 标题 + 时长（衬线等宽数字）。无标签、无优先级、无分组。 |
| M | 同 C。行高从 60 收到 56（鼠标比手指精确），命中区仍 ≥44。 |
| E/W | 主列仍是这个列表；**新增悬停行内动作**：鼠标悬停某行时行尾淡入「今天先不做」（不着红、不加删除线）。**键盘**：`J/K` 或 `↑/↓` 移动焦点、`空格` 勾选、`L` 今天先不做。辅栏显示：今天共 N 件 / 合计 X 分钟 / 已完成 Y 件。 |

「今天先不做」在手机上**不放行内**——放在页面底部那句「今天先做这些，剩下的明天再说」（现状已有），以及长按行时的确认。理由是手机上一行里塞两个可点元素会让命中区互相挤压。

### 7.3 `goals` —— 我的目标

| 档 | 形态 |
|---|---|
| C | 一句话标题 → 大输入框（只有底线，不是盒子）→ 4 条示例（发丝线分隔的句子列表）→ 主按钮 → 一行"提交后会依次经过 6 个阶段"，**只在真实到达时点亮**（不伪造进度）。 |
| M | 同 C。 |
| E/W | 主列 = 输入框 + 主按钮（示例移到**右辅栏**，保持"句子列表"形态，点击后**不消失**）。辅栏下半部：已提交过的目标，每条带最新计划版本号与"看拆解"入口。 |

### 7.4 `assessment` —— 我现在的状态

| 档 | 形态 |
|---|---|
| C | 三个问题竖排，每组之间一条发丝线：精力 4 档 → 心情 4 档 → 压力 4 档。提交按钮文案随已答数量变化（现状已实现）。「今天不填了」与主按钮视觉权重接近。 |
| M | 同 C。 |
| E/W | 三组仍竖排（**不做两栏**：竖排一屏能看完全部，拆栏只会让人来回扫）。辅栏显示：① 上次填写是"昨天 21:40" ② 一句说明——"心情这一项只影响今天的安排顺序，不会改任务量"。 |

### 7.5 `review` —— 上周回顾

| 档 | 形态 |
|---|---|
| C | 结论句 + 大数字 `8 / 12` → 横向条形图（三条：上周安排 / 上周完成 / 本周安排）→ 归因段 + 调整门槛 → `[还是按原来的量来吧]`。 |
| M | 同 C，条形更长（640）。 |
| E/W | 主列 = 大数字 + 结论句 + 回退按钮；**右辅栏** = 三条条形 + 归因段 + 门槛说明。即：把"结论"留在主列，"依据"放进辅栏——这比现在两者叠在一列更符合"先给答案，再给依据"的读法。 |

### 7.6 还没做进本方案的一页

**AI 生成等待态（6 阶段步骤条）**现在是 `goals.tsx` 底部一行静态文案。它应该做成 `StageProgress` 组件（SSE stage 名 = 节点名，直接 map），**留到接后端时做**——它必须消费真实事件，现在做等于写假进度，而调研报告里明确写了「绝不伪造进度」。

---

## 8. 组件清单

新增/改动的组件，全部双端共用（除 `shell*`）：

| 组件 | 文件 | 职责 | 平台差异 |
|---|---|---|---|
| `AppShell` | `components/shell.tsx` | 根布局：导航 + 内容网格；接安全区与窗口 | Web/原生两支，**唯一**允许分平台的地方 |
| `ContentGrid` | `components/shell.tsx` | 主列 + 辅栏的 4 档网格；辅栏在窄档下移 | 无（靠 `useLayout()`） |
| `NavRail` | `components/nav-rail.tsx` | 取代现在的 `tab-bar.tsx`：窄档底栏 / 宽档竖栏 | 无（靠 `useLayout()`） |
| `Page`（改） | `components/page.tsx` | 加 `detail` 槽；主列 maxWidth 统一 760 | 无 |
| `Rule` | 现状保留 | 发丝线，**平面层的分区手段** | 无 |
| `Surface`（新） | `components/surface.tsx` | **本方案新增的核心组件**：统一"面"的容器。props：`elevation="flat\|raised\|overlay\|inset"` + `radius="sm\|md\|lg\|xl\|overlay"` + `variant="solid\|glass"`。阴影、暗色叠白、圆角、毛玻璃降级全部封在这里 | 有（阴影与 `backdrop-filter` / `BlurTargetView` 分 Web/原生），**差异不外泄** |
| `DetailPanel`（新） | `components/detail-panel.tsx` | 辅栏容器：标题 + 若干块；宽档 sticky | sticky 仅 Web |
| `WhyNote`（新） | `components/why-note.tsx` | 「为什么是这件事」：左竖线 + 归因文字（`index`/`review` 共用） | 无 |
| `TouchRow`（新） | `components/touch-row.tsx` | 统一的可点/可聚焦行：`Pressable` + 悬停叠层 + 焦点环 + 可选行尾动作 | 无 |
| `FocusRing`（新） | 随 `touch-row.tsx` 导出 | 焦点可见性（仅 `keyboard` 能力为真时渲染） | 无 |
| `StageProgress`（新） | `components/stage-progress.tsx` | 6 阶段步骤条，消费 SSE stage 名 | 无 |
| `LevelPicker` | 现状保留 | 4 档选择 | 无 |
| `Bar` | 现状内联在 `review.tsx` | 横向条形 → 提到 `components/bar.tsx` 供复用 | 无 |
| `TopBar`（新） | `components/shell.tsx` | 手机顶部标题行（44px + 安全区）；Web 不渲染 | 有，封在 shell 内 |

**组件规则**（写进注释，供接手的人）：

1. 组件**只从 `useTheme()` 取色**，一个颜色字面量都不许出现。
2. **阴影只有一个入口**：所有阴影、暗色下的叠白替代、毛玻璃降级都在 `Surface` 里实现，组件**不许自己写阴影**。亮度档只有 `flat / raised / overlay / inset` 四档（见 5.2），跨端统一用 `boxShadow`（RN 0.86 对 iOS 与 Android 都支持，含 `inset`），**不要**退回 `shadowOffset/shadowOpacity/shadowRadius`（那三个只有 iOS 生效）。
3. **平面层（列表行、区块、图表）仍然用发丝线**，不要顺手加阴影。让阴影只表达"浮起来"这一件事。
4. 圆角只允许 4 / 8 / 12 / 16 / 20 / full，且**跟着层级走**：越大越浮（见 5.3）。
5. 任何可点元素：`minHeight: 44`（触屏）或 `32`（仅鼠标且 `touch === false`）。
6. **焦点环不要依赖浏览器默认行为，也不要写 CSS 伪类**：`FocusRing` 的实现方式是 `Pressable` 的 `onFocus` / `onBlur` 翻转一个 boolean，再在元素外层叠一条 2px 的 `accentLine` 描边。这样 Web / 原生 / 将来 RNW 三端行为一致（RN 每个可聚焦元素都会发这两个事件），也不必碰 `outline` 这种 Web-only 属性。

---

## 9. 交互模型：三种输入方式一张表

| 行为 | 触屏（手机） | 鼠标（桌面 Web） | 键盘（桌面 Web） |
|---|---|---|---|
| 切换页面 | 点底部标签 | 点左侧竖栏 | `Ctrl + 1..5` |
| 查看「为什么」 | 点卡片下方展开 | 悬停即见（辅栏常驻） | 辅栏始终可见 |
| 开始 / 完成 | 点 `[开始]` | 同左 | `Enter` / `空格`（焦点在任务上） |
| 今天先不做 | 页面底部那一句 / 长按行 | **行尾淡入的按钮** | 焦点在该行时 `L` |
| 列表上下移动 | 手指滑动 | 滚轮 / 拖动滚动条 | `J`/`K` 或 `↑`/`↓` 移动行焦点 |
| 返回上游页 | 系统返回手势 / 点标签 | 点左侧竖栏 | `Esc`（等价于「今天不填了」/「返回」） |
| 关闭弹层 | 点遮罩 / 下滑 | 点遮罩 / 点空白 | `Esc` |

硬性纪律：

- **快捷键永远不是唯一入口**：上表每个快捷键对应的动作，触屏那一列都有对应做法。
- **`Esc` / `Enter` / `空格` 不得劫持输入框**：`goals.tsx` 的多行输入框聚焦时，全局快捷键必须放行。这是最容易写错的一处。
- **不加任何 JS 驱动的布局动画**（现状已定的纪律）：动效只允许 CSS `transition`，且只动 `transform` / `opacity`；原生端与 RNW 端不做过渡，直接切换。
- **"凸 → 凹"的切换不做过渡**：只在 Web 上可以给 `boxShadow` 加 transition（浏览器能合成），原生端直接切换。这条要把第 5.2 节的 `raised` / `inset` 两档做成**离散状态**，而不是让组件去插值阴影。
- 焦点顺序 = DOM 顺序 = 视觉顺序。`NavRail` 在最前，所以 `Tab` 键第一次落点在导航上——这与桌面软件惯例一致。

---

## 10. 状态与数据：让页面"换壳不丢东西"

现在 5 个页面各自 `useState` 假数据。这带来一个桌面上特别刺眼的问题：**页面切换会丢状态**（在 `tasks` 勾了一半，切到 `goals` 再回来，勾选没了）。手机上还能忍，桌面上用户会频繁切页，忍不了。

| 状态 | 归属 | 双端共有 |
|---|---|---|
| 当前计划、任务、为什么、上周回顾 | 后端（`plan` + `assessment`） | ✅ 同一份 |
| 今天的勾选/跳过（乐观更新） | `AppStateProvider` | ✅ 同一份 |
| 自评草稿（填了两项还没提交） | `AppStateProvider`（表单域单独一片） | ✅ 同一份 |
| 辅栏展开/收起 | 组件本地 | ✅（只是形态差） |
| 窗口宽度、输入能力 | `CapabilitiesProvider` | 平台提供 |

**判据**：`index` / `tasks` / `review` 三个页面必须是 `f(plan, assessment, now)` 的纯函数——同样的数据在任何设备、任何宽度上渲染出同样的内容。这条决定了双端不会漂移。

---

## 11. 可达性与排版底线（双端一份）

| 项 | 标准 | 说明 |
|---|---|---|
| 命中区 | ≥ **44×44pt**（触屏）；鼠标环境可到 32 | WCAG 2.2 SC 2.5.8 的 24×24 是**下限**，不是目标 |
| 焦点可见 | 每个可聚焦元素有 2px `accentLine` 焦点环、2px 偏移 | **本方案要补的头号缺口** |
| 正文对比度 | ≥ 4.5:1（正文）· ≥ 3:1（≥24px 或加粗） | 现存 `textFaint` 未达标，见 4.1 与附录 C |
| 正文行宽 | ≤ 80 字符（≈760px @16px） | 双端一致，桌面不放宽 |
| 中文行高 | ≥ 1.5 | 桌面取 1.5，手机 1.6 |
| 字重 | 只用 400 / 700 | Android 中文只有两档 |
| 字体 | 中文一律系统字体；拉丁与数字用 Newsreader（Web）/ 平台衬线（原生） | 不加载中文字体 |
| 动效 | 尊重 `prefers-reduced-motion`；不超过 300ms | 现有 `Motion` token |
| 暗色 | 不用纯黑（`#15171A`） | 现状已对 |
| 缩放 | 不关 `allowFontScaling` | 布局一律 `minHeight`，不用固定 `height` |

---

## 12. 落地顺序（每步都能单独看效果）

| 步 | 做什么 | 完成判据（你自己能验证） |
|---|---|---|
| 1 | 加 `constants/tokens.ts`（含 `elevation` 四档、`radius` 六档、`surface1/2/3`）；`theme.ts` 补 `Spacing.nine`；把 `document.createElement` 的字体加载收进 `constants/fonts.web.ts` | 浏览器与手机上画面**没有任何变化**（纯结构改动） |
| 2 | 加 `components/surface.tsx`（`flat/raised/overlay/inset` × `solid/glass`），先把 `review.tsx` 的结论块换成 `Surface elevation="raised"`、把 `assessment.tsx` 选中的档位换成 `elevation="inset"` 各试一次 | 那一块出现柔和阴影；选中的档位**凹**下去；**切到暗色，凸起换成叠白而不是看不见**；列表行仍然没有阴影 |
| 3 | `tab-bar.tsx` → `nav-rail.tsx`，宽度档从 2 档改 4 档；宽档的导航栏套 `Surface variant="glass"` | 拖窄/拖宽浏览器窗口，导航在 <700 是底栏、≥700 是竖栏；内容从导航栏下面滚过时能看到通透 |
| 4 | 加 `CapabilitiesProvider`（悬停/键盘/触屏）与 `FocusRing` | 用 `Tab` 键走一遍，每个可点处都能看到金色细环 |
| 5 | `Page` 加 `detail` 槽与 `ContentGrid`；`_layout.tsx` 换成 `AppShell` | ≥1100px 时出现右侧列（先放占位文字） |
| 6 | 改 `index.tsx`：辅栏接「为什么」与「接下来」 | 窗口从 1440 拖到 375 再拖回来，信息一项不少、只是换位置 |
| 7 | 改 `tasks.tsx`（悬停行内动作 + 键盘移动）、`review.tsx`（依据进辅栏）、`goals.tsx`（示例进辅栏）、`assessment.tsx`（辅栏补上次填写时间） | 每页在两个宽度下各看一遍 |
| 8 | 键盘映射表 + `goals` 输入框放行规则 | 在输入框里打字不会触发快捷键 |
| 9 | （页面定稿后）Tauri 2 套壳 | 见第 14 节 |

**第 1–4 步做完，双端统一的地基与视觉语言就都成立了**（第 2 步是阴影/圆角/玻璃的落地，第 3 步顺带把玻璃用在导航栏上）；第 5–8 步是布局与交互的可见收益。

> 如果时间不够，砍的顺序是：**8 → 7 → 6**。**1–5 不能砍**，尤其第 2 步（`Surface`）——没有它，阴影和圆角会散落在各个页面里，之后再也收不回来。

---

## 13. 已知代价与"不做"

**要接受的代价**：

1. 桌面端**永远不是原生 Windows 手感**（没有 Fluent 系统控件、没有任务栏跳转列表）。换来的是零新增技术栈与一份代码。
2. 宽屏上辅栏"多出一列"，会让截图左右不对称。这是刻意的：**信息比构图重要**。
3. 60px 竖栏放不下完整中文标签，只能放 2–3 字（「现在做什么」→「现在」，「我现在的状态」→「状态」）。**同一份文案不能两套**——建议 `TABS` 加 `shortLabel` 字段，`label` 不动（底栏继续用完整短语）。
4. 桌面 Web 的性能不如原生，长列表滚动会有差异。缓解：列表只渲染可见项、避免 JS 滚动监听。

**明确不做**：

| 不做 | 原因 |
|---|---|
| 独立 Web 控制台（表格/多栏筛选） | 五个页面 3 天工期，第二套导航的调试成本 > 收益（《双端前端架构调研》第 5 节已论证） |
| 深浅色手动切换的持久化 | 跟系统即可；要做就得写 Context + 存储 + 防闪，收益低于成本 |
| **大范围毛玻璃 / 卡片毛玻璃 / 弹层背景模糊** | 见 5.5：Android 上要么不支持、要么低端机掉帧；且半透明层叠滚动文字会破坏对比度。**只保留导航栏两处** |
| **每个区块都做成圆角卡片 + 阴影** | 见 5.2 纪律 1：那是模板脸，而且手机端一屏可见条数会从 6–8 掉到 3 |
| **渐变** | 与"面 + 线"的语言冲突；且调研文档把渐变列为速朽做法 |
| 图标库 | 竖栏靠文字 + 强调线已经够分辨；引入图标要同时维护三端图标集 |
| 骨架屏 | 调研报告里有对照实验证据：骨架屏在"加载很快"的认同率上全指标最差（59% vs spinner 74%） |

> 注意：初版方案里"阴影、圆角、玻璃一律不用"这条**已经被第 5 节取代**。现在的规则不是"不用"，而是"**按层次用，且只有一档**"。

---

## 14. 交付形态（已定）

### 14.0.2 v3：一轮实拍驱动的外观修正（反馈 → 证据 → 改动）

这一轮的每一条都不是"我觉得"，而是**先截图/量数据、再改**。留下证据链，避免下一个人凭感觉回退。

| 反馈 | 现场证据 | 结论与改动 |
|---|---|---|
| **"侧边栏和底栏怪怪的"** | 1440 下侧栏被推到**屏幕最右**、左边空一条 250px 灰带；390 下底栏 5 个完整短语**字压字**；导航**没有底色**，文字直接压在内容上 | 侧栏 208 → **76px 只放 2 字短标签**；底栏也改用短标签并**均分**；导航整块改成**玻璃面**（有底色、有切边、有上缘线）。`tabs.ts` 新增 `shortLabel`，`label` 保留给无障碍 |
| **"毛玻璃太厚重、不够通透"** | alpha 0.72 / 0.70 = 一块**不透明实色板**，模糊层被实色盖住 | alpha 降到 **0.52 / 0.46**，模糊半径 18 / 16，加 `saturate` 补偿。实测计算值：`backdrop-filter: blur(18px) saturate(1.5)`（浅）/ `blur(16px) saturate(1.4)`（深）。通透感来自"**能看见背后**"，不是模糊强度 |
| **"配色年轻一些"** | 旧金 `#8A5A0B` 偏棕、像皮革；旧 sage `#4A6B4F` 灰而闷；中性底 `#F2F4F6` 偏绿偏脏 | 底 → `#F5F7FA`（更亮更冷）、墨 → `#12151B`；「现在」拆成**填充 `#C67C10`**（鲜）+ **文字 `#A8670A`**（4.25:1）；「完成」→ 薄荷 `#0F9D63`。暗色对应 `#FFC24B`（11.5:1）/ `#4ADE9B`（10.7:1） |
| **"加手动深浅切换"** | 只能跟系统，没法演示 | 新增 `hooks/use-theme-mode.tsx`：三态 **浅色 → 深色 → 跟系统**，`localStorage` 持久化，原生侧用官方 `Appearance.setColorScheme()` 同步。**额外加了 `?theme=light\|dark` 的 URL 覆盖**，因为截图验收需要确定性取值 |
| **"按钮有 bug"** | 见下 | 两处真错，已修 |

**按钮的两个真错**（这一条值得单独记）：

1. **主按钮底色取错 token。** `accent` 是"能当**文字**用"的深琥珀，被我拿去当**填充**底色了。v3 把角色拆开：`accentFill` 管填充、`accent` 管文字与线。顺带发现浅色下 `onAccent` 应该是**深墨而不是白** —— 白字在 `#C67C10` 上只有 3.34:1，深墨有 5.1:1，而且深字压暖饱和色更亮、更年轻。
2. **按钮不等宽**。只给了 `paddingHorizontal`，于是一行里两个按钮一宽一窄显得歪。加了 `minWidth: 116`。禁用态也从 `opacity 0.4` 收到 `0.38`，避免看着像"淡了一点的正常按钮"。

> 顺带纠正一个我自己的误判：暗色下按钮**计算值 `boxShadow: none` 不是 bug** —— 暗色按设计就不投阴影，改用"顶部高光内描边"。当时的 `border: 1px rgba(236,238,240,0.06)` 正是那条高光。**取证前先确认自己看的主题**。

| 端 | 形态 | 说明 |
|---|---|---|
| **开发 / 测试** | Web（`npx expo start --web`） | UI 一律先在浏览器里调；拖窗口改宽度就是响应式测试 |
| **Windows 正式版** | **Tauri 2 套壳** | 加载的就是同一份 Web 构建产物，**UI 代码零改动** |
| **手机** | 按目前（Expo / Android） | 不变 |

### 14.0 我怎么能"看到"页面（视觉自检链路）

之前我只能读 DOM 和代码，看不到画面 —— 这在这种"质感/层次"的设计上等于瞎调。现在打通了，**零新增依赖**：用系统自带的 Edge 无头模式截图。

| 手段 | 命令要点 |
|---|---|
| 单张截图 | `msedge.exe --headless --disable-gpu --window-size=1440,1000 --screenshot=out.png <url>` |
| **强制明/暗** | 加 `--blink-settings=preferredColorScheme=1`（浅）/ `=0`（深）。⚠️ `--force-dark-mode` **没用**，它不改变 `prefers-color-scheme` |
| 量真实布局 | `damn-app\.shots\measure.js`（CDP，零依赖）：打印视口、`scrollWidth`、溢出元素清单、指定元素的 `getBoundingClientRect` + 计算样式，并可出全页截图 |

用法：`node .shots/measure.js <url> <宽> <高> [输出png] [1=浅 0=深]`

> ⚠️ **一个前提**：Chromium 靠**命名管道**做多进程 IPC，受限沙箱下会直接崩（`FATAL:mojo platform_channel Check failed: 拒绝访问 0x5`），症状是进程退出码 `0x80000003` 而截不出图。本会话已放开文件沙箱，所以可用。

### 14.0.1 靠这条链路抓到的两个真 bug（已修，都记在这别重犯）

| # | 症状 | 真因 | 修法 |
|---|---|---|---|
| 1 | **中文冲出容器被裁掉**（长句一直画到屏幕边缘） | react-native-web 把每个 `<Text>` 渲染成带 **`white-space: pre-wrap`** 的 div；在 `pre-wrap` 下**中文不按字符断行** | `global.css` 里加 `#root, #root * { white-space: normal !important }`（必须 `!important`，RNW 写的是内联样式） |
| 2 | **卡片全部叠在同一坐标上**（网格退化成一条竖线） | 我给容器设的 `flexDirection: 'row'` + `flexWrap: 'wrap'` **被 RNW 的样式合并吃掉**，计算值落回 `column` + `nowrap` | 新增 `CardGrid`：**按列数显式分行**，且自己按窗口宽度算实际列数与格宽，不依赖 flex 换行 |

两条都是**跨端纪律的延伸**，要补进第 3 节的红线里：

> **红线 4：不要依赖 `flexWrap` 做布局。** 需要换行就按列数显式分行（用 `CardGrid`）。RNW 下 `flexWrap` 不可靠。
>
> **红线 5：中文文本容器的换行由 `global.css` 兜底**，别在组件里去改 `white-space`；`pre-wrap` 是 RNW 默认值，不要以为 RN 的换行行为在 Web 上自动成立。

### 14.1 为什么 Tauri 而不是 Electron / RNW

- **RNW 已被否掉**（第 1 节）：不是工作量问题，是 `expo-modules-core` 在 Windows 上没有运行时。
- **Tauri vs Electron**：两者都用"套同一份 Web 构建"，所以对我们的**代码**没有区别。差别只在体积（几 MB vs 上百 MB）和内存。既然 UI 完全不改，选体积小的那个。

### 14.2 打包前必须验的三件事（现在**不要**动配置）

| # | 风险 | 现状（我实测过） | 处理 |
|---|---|---|---|
| 1 | **静态导出是"每个路由一个 HTML"**（8 个文件），不只是 SPA | `npx expo export --platform web` 产出 `index.html / tasks.html / goals.html / assessment.html / review.html / workbench.html …` | Tauri 里客户端路由跳转可能拿不到对应文件 → 打包阶段评估是否把 `web.output` 改成 `single`（SPA，单 `index.html`）。**这是构建配置改动，等页面定稿再动** |
| 2 | **资源引用是绝对路径** | 导出产物里是 `/_expo/static/js/web/entry-*.js`，不是 `./_expo/...` | Tauri 的 `frontendDist` 指向 `dist` 根目录时 `/` 应当能命中；若白屏，第一件事就是查这条。备选是给 Expo 配 `baseUrl: './'` |
| 3 | **WebView2 运行时** | **我没实测** | Win11 基本自带；Win10 视版本可能要装一次，或把运行时打进安装包（代价是体积） |

> 结论：**现在只在浏览器里开发和验证**。上面三条都留到"页面定稿 + 要出 .exe"的时候集中处理，因为它们动的是构建配置而不是 UI 代码。

### 14.3 样式参考页（已实现，用来定样式）

页面结构还在讨论，所以先做了一个**样式参考页**：`damn-app/src/app/workbench.tsx`，浏览器里访问 `/workbench`。
它不在 `constants/tabs.ts` 里，所以**不会出现在导航栏上**，随时可以整页删掉。

它把这套设计里"可能会用到的组件和功能"摆在一屏，用来回答三个只有看实物才能回答的问题：

1. 轻拟物的**凹 / 平 / 凸 / 浮**四档，在真机上分不分得出来；
2. 毛玻璃在**深色**下到底好不好看（就是那个顾虑，见 5.5 的实测表）；
3. 字阶 + 金 / 鼠尾草的语义独占，合起来还像不像 DAMN，而不是"又一个 Material App"。

覆盖的组件：`Surface`（四档高度 × 六档圆角 × 玻璃/实心）、`Button`（主 / 次 / 危险 / 禁用 / 两种尺寸）、`Row`（悬停出行内动作 + 焦点环）、`Segmented`（选中即凹）、`StageProgress`（6 阶段，名 = 后端节点名）、`Stat`、`Swatch`、输入框（聚焦即凹）、空态 / 错误 / 提示三种反馈块，以及焦点验证块。
落地时**新增组件的样式一律从这里抄**，别各自发挥。

配套新增文件：

| 文件 | 作用 |
|---|---|
| `src/constants/tokens.ts` | 高度四档、圆角六档、表面色、玻璃（含深色实测结论）、交互叠层、焦点环 |
| `src/components/surface.tsx` | 阴影 / 暗色叠白 / 圆角 / 毛玻璃降级的**唯一实现处** |
| `src/components/nav-rail.tsx` | 导航壳：窄档底栏 / 宽档 76px 竖栏，玻璃面，短标签 |
| `src/components/controls.tsx` | `Button` / `Row` / `Segmented` / `StageProgress` / `Stat` / `Swatch` / `CardGrid` / `ThemeToggle` / `Section` |
| `src/hooks/use-theme-mode.tsx` | 手动明暗三态 + `localStorage` + `?theme=` 覆盖 |
| `src/app/workbench.tsx` | 样式参考页（不接入导航，可整页删除） |

---

## 附录 A：与现状的具体偏离点（改代码时对照）

| 文件 | 现状 | 要改成 |
|---|---|---|
| `app.json` | `orientation: "portrait"` | 删除该字段（否则桌面窗口被锁成窄条） |
| `src/app/_layout.tsx` | 自带 `document.createElement` 加载字体；`flexDirection` 直接按 `isWide` 切 | 字体搬到 `constants/fonts.web.ts`；布局交给 `AppShell`，本文件只留 `ThemeProvider` + `SplashScreen` |
| `src/components/tab-bar.tsx` | 208px 侧栏 / 底栏，2 档 | 改名 `nav-rail.tsx`，60px 竖栏 / 底栏，4 档 |
| `src/components/page.tsx` | `maxWidth` 640/760，无辅栏 | 主列统一 760，新增 `detail` 槽；E/W 档包 `ContentGrid` |
| `src/constants/tokens.ts`（新增） | 不存在 | 放 `elevation` 四档（flat/raised/overlay/inset）、`radius` 六档、`surface1/2/3`、`LineHeight.dense`、`layoutMode()`（见第 4、5 节的表） |
| `src/components/surface.tsx`（新增） | 不存在 | 阴影 / 叠白 / 圆角 / 毛玻璃降级的**唯一实现处**，组件不许自己写阴影 |
| `src/constants/theme.ts` | `Breakpoints = { sidebar: 768, wide: 1100 }` | 改为 4 档：`compact 700 / expanded 1100 / wide 1440` |
| `src/app/index.tsx` | 刻度尺 + 全部内容一列 | 辅栏接「为什么」+「接下来」；窄屏刻度尺收成一行 |
| `src/app/tasks.tsx` | 只有勾选 | 加悬停行内「今天先不做」+ 键盘移动焦点 |
| `src/app/review.tsx` | 结论与依据同列 | 依据移入辅栏（窄屏仍在下） |
| `src/app/goals.tsx` | 示例在主列 | E/W 档示例移入辅栏 |
| 全项目 | 无 focus ring | 每个可聚焦元素加 `FocusRing` |

## 附录 B：以后再说（留档，不实现）

- 全屏专注模式（只剩当前这一件 + 计时）：演示价值最高，但它引入"第二个页面"，等 5 页闭环跑通再评估。
- 本地通知（"到点做某件"）：`expo-notifications` 本地通知在 Expo Go 里可用，双端都有等价物，属于功能不属于 UI。
- 迷你浮动窗、任务栏进度、系统级快捷提醒：只在 RNW 落地后才成立。

## 附录 C：需要你拍板的点

| # | 决策点 | 选项 | 我的建议 |
|---|---|---|---|
| 1 | `textFaint`（浅 `#9AA0A9` / 暗 `#6E747C`）实测对比度只有 2.39:1 与 3.81:1，低于 AA 4.5:1；而它用在时间戳、完成态标题这类要读的文字上 | A. 浅暗两色都提到 `textMuted` 那一档（承认没有第四级文字）B. 保留 `textFaint`，但**只允许非文字用途**（分隔线/刻度/图标），文字一律 `textMuted` | B。成本是「精力中等」和删除线标题提一档灰度，收益是保住层次且过 AA |
| 2 | 60px 竖栏里放 2–3 字短标签 | A. `TABS` 加 `shortLabel` 字段 B. 竖栏隐藏文字只留强调线 | A（B 会让新用户看不出这栏是干什么的） |
| 3 | 4 档断点是否就按 `700 / 1100 / 1440` | A. 就按这个 B. 你来定 | A。700 与现状 768 接近，改动最小 |
| 4 | 毛玻璃只留**导航栏两处**（桌面左栏 + 手机底栏），还是你想要更多？ | A. 就两处（5.5 的方案）B. 再加弹层/卡片 | A。Android 上要么不支持要么掉帧，且玻璃越多越像模板 |
| 5 | 抬升面用「一档真阴影」还是干脆全用发丝线？ | A. 四档（平面/抬起/浮层/凹，5.2 的方案）B. 只保留浮层一档，其余全平面 | A。桌面的四层结构需要它；若你担心"变模板"，可以从 B 起步再放开 |
| 6 | **"凹"（inset）这一档要不要**：它是轻拟物和"悬浮卡片"的分界，也是我这里唯一对拼色/质感方案加的硬约束（凸=可点、凹=已发生） | A. 要，且**凸起只给真正可点的元素**（5.2 的方案）B. 要凹，但凸起也可以给静态区块 | **A，这条我建议不要放宽。** 凸起是交互信号；一旦静态内容也凸起，用户会去点它，而且手机端一屏可见条数会掉一半 |

### 已拍板（不需再决策）

| # | 事项 | 结论 | 记录位置 |
|---|---|---|---|
| D1 | Windows 端形态 | 开发/测试走 **Web**；正式版 **Tauri 2 套壳**，UI 代码零改动 | 第 14 节 |
| D2 | 手机端 | 按目前不变 | 第 14 节 |
| D3 | 深色毛玻璃 | 叠**"抬升白" `#2A2E33` @0.70**（不是纯白）；白玻璃在深色下只有 1.60:1，读不了 | 5.5 实测表 |
| D4 | 实施节奏 | 页面还在讨论 → 先落**样式参考页** `/workbench`，把组件与质感先定死 | 14.3 |

## 参考

- [React Native for Windows · Support Policy](https://microsoft.github.io/react-native-windows/support/) · [System Requirements](https://microsoft.github.io/react-native-windows/docs/rnw-dependencies) · [Get Started](https://microsoft.github.io/react-native-windows/docs/getting-started)
- 版本对照（都是 npm 一手数据）：[react-native-windows@0.84.0](https://registry.npmjs.org/react-native-windows/latest)（peer `react-native@0.84.1`）· [react-native-windows@0.83.0](https://registry.npmjs.org/react-native-windows/0.83.0)（peer `react-native@^0.83.0`）· [expo@55.0.31](https://registry.npmjs.org/expo/55.0.31)（RN 0.83.10）· [expo@56.0.22](https://registry.npmjs.org/expo/56.0.22)（RN 0.85.3）
- `expo-modules-core` 没有 Windows 实现：[仓库目录](https://github.com/expo/expo/tree/main/packages/expo-modules-core)（只有 `android/` `ios/` `common/`）与它自己的 [`react-native.config.js`](https://github.com/expo/expo/blob/main/packages/expo-modules-core/react-native.config.js)（显式关掉 Windows autolinking）
- [Expo docs: Install Expo modules in an existing React Native project](https://docs.expo.dev/bare/installing-expo-modules/)（只有 Android / iOS 配置）
- [expo/expo#47703 — Proposal: recognize react-native-windows as an out-of-tree platform](https://github.com/expo/expo/issues/47703)
- [React Native Directory（Windows 过滤）](https://reactnative.directory/?windows=true)
- [React Native 0.86 · Shadow Props](https://reactnative.dev/docs/0.86/shadow-props)（三套阴影 API 的支持范围：`boxShadow` 支持 iOS + Android 且含 `inset`；`shadow*` 老属性除 `shadowColor` 外仅 iOS）
- [Expo SDK 57 · BlurView](https://docs.expo.dev/versions/v57.0.0/sdk/blur-view.md)（平台字段 Android/iOS/tvOS/Web；Android 的 `BlurTargetView` 接线、SDK 31 的 RenderNode 性能边界、`FlatList` 已知问题）
- 本项目：《DAMN-UI调研报告.md》《DAMN-双端前端架构调研.md》《docs/research/cross-platform-visual-design-systems.md》
